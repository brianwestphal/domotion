// AUTO-GENERATED by scripts/build-studio-client.mjs — do NOT edit by hand.
// The source lives at src/studio/client.tsx; rerun the build to regenerate.

export const STUDIO_CLIENT_JS: string = "\"use strict\";\n(() => {\n  // node_modules/kerfjs/dist/chunk-YHH7OUFA.js\n  var ROW_HTML_SNIPPET_MAX = 120;\n  function truncateRowHtml(html) {\n    return html.length > ROW_HTML_SNIPPET_MAX ? html.slice(0, ROW_HTML_SNIPPET_MAX) + \"\\u2026\" : html;\n  }\n  var SVG_NS = \"http://www.w3.org/2000/svg\";\n  var MATHML_NS = \"http://www.w3.org/1998/Math/MathML\";\n  var SVG_HTML_INTEGRATION = /* @__PURE__ */ new Set([\"foreignObject\", \"desc\", \"title\"]);\n  var MATHML_TEXT_INTEGRATION = /* @__PURE__ */ new Set([\"mi\", \"mo\", \"mn\", \"ms\", \"mtext\"]);\n  function foreignWrapper(parent) {\n    if (parent == null) return null;\n    const { namespaceURI, localName } = parent;\n    if (namespaceURI === SVG_NS) {\n      return SVG_HTML_INTEGRATION.has(localName) ? null : \"svg\";\n    }\n    if (namespaceURI === MATHML_NS) {\n      return MATHML_TEXT_INTEGRATION.has(localName) ? null : \"math\";\n    }\n    return null;\n  }\n  function parseRowTemplate(html, parent) {\n    const tpl = document.createElement(\"template\");\n    const wrapper = foreignWrapper(parent);\n    if (wrapper === null) {\n      tpl.innerHTML = html;\n      return { content: tpl.content, count: tpl.content.children.length };\n    }\n    tpl.innerHTML = `<${wrapper}>${html}</${wrapper}>`;\n    const host = tpl.content.firstElementChild;\n    const content = document.createDocumentFragment();\n    while (host.firstChild !== null) content.appendChild(host.firstChild);\n    return { content, count: content.children.length };\n  }\n  function parseSingleRow(html, index, parent) {\n    const { content, count } = parseRowTemplate(html, parent);\n    if (count !== 1) throw rowContractError(index, html, parent);\n    return content.firstElementChild;\n  }\n  function collectTemplateChildren(content, n2) {\n    const nodes = new Array(n2);\n    let child = content.firstElementChild;\n    for (let k = 0; k < n2; k++) {\n      nodes[k] = child;\n      child = child.nextElementSibling;\n    }\n    return nodes;\n  }\n  function rowContractError(index, html, parent) {\n    const { count } = parseRowTemplate(html, parent);\n    const reason = count === 0 ? \"produced no top-level element\" : `produced ${count} top-level elements; exactly one is required`;\n    return new Error(\n      `each(): row render at index ${index} ${reason}. Each item's render must return exactly one element \\u2014 wrap multiple roots in a single parent (e.g. <li>...</li>). Got HTML: ${JSON.stringify(truncateRowHtml(html))}`\n    );\n  }\n\n  // node_modules/kerfjs/dist/chunk-VVDJLWMP.js\n  var devHooks = {};\n\n  // node_modules/@preact/signals-core/dist/signals-core.module.js\n  var i = /* @__PURE__ */ Symbol.for(\"preact-signals\");\n  function t() {\n    if (!(v > 1)) {\n      var i2, t2 = false;\n      !(function() {\n        var i3 = c;\n        c = void 0;\n        while (void 0 !== i3) {\n          var t3 = i3.S;\n          if (t3.v === i3.v) {\n            for (var n3 = t3.t; void 0 !== n3; n3 = n3.x) if (n3.i === i3.i) n3.i = t3.i;\n          }\n          i3 = i3.o;\n        }\n      })();\n      while (void 0 !== h) {\n        var n2 = h;\n        h = void 0;\n        s++;\n        while (void 0 !== n2) {\n          var r2 = n2.u;\n          n2.u = void 0;\n          n2.f &= -3;\n          if (!(8 & n2.f) && w(n2)) try {\n            n2.c();\n          } catch (n3) {\n            if (!t2) {\n              i2 = n3;\n              t2 = true;\n            }\n          }\n          n2 = r2;\n        }\n      }\n      s = 0;\n      v--;\n      if (t2) throw i2;\n    } else v--;\n  }\n  var r;\n  var o = void 0;\n  function f(i2) {\n    var t2 = o, n2 = r;\n    o = void 0;\n    r = void 0;\n    try {\n      return i2();\n    } finally {\n      o = t2;\n      r = n2;\n    }\n  }\n  var h = void 0;\n  var v = 0;\n  var s = 0;\n  var e = 0;\n  var c = void 0;\n  var d = 0;\n  function a(i2) {\n    if (void 0 !== o) {\n      var t2 = i2.n;\n      if (void 0 === t2 || t2.t !== o) {\n        t2 = { i: 0, S: i2, p: o.s, n: void 0, t: o, e: void 0, x: void 0, r: t2 };\n        if (void 0 !== o.s) o.s.n = t2;\n        o.s = t2;\n        i2.n = t2;\n        if (32 & o.f) i2.S(t2);\n        return t2;\n      } else if (-1 === t2.i) {\n        t2.i = 0;\n        if (void 0 !== t2.n) {\n          t2.n.p = t2.p;\n          if (void 0 !== t2.p) t2.p.n = t2.n;\n          t2.p = o.s;\n          t2.n = void 0;\n          o.s.n = t2;\n          o.s = t2;\n        }\n        return t2;\n      }\n    }\n  }\n  function l(i2, t2) {\n    this.v = i2;\n    this.i = 0;\n    this.n = void 0;\n    this.t = void 0;\n    this.l = 0;\n    this.W = null == t2 ? void 0 : t2.watched;\n    this.Z = null == t2 ? void 0 : t2.unwatched;\n    this.name = null == t2 ? void 0 : t2.name;\n  }\n  l.prototype.brand = i;\n  l.prototype.h = function() {\n    return true;\n  };\n  l.prototype.S = function(i2) {\n    var t2 = this, n2 = this.t;\n    if (n2 !== i2 && void 0 === i2.e) {\n      i2.x = n2;\n      this.t = i2;\n      if (void 0 !== n2) n2.e = i2;\n      else f(function() {\n        var i3;\n        null == (i3 = t2.W) || i3.call(t2);\n      });\n    }\n  };\n  l.prototype.U = function(i2) {\n    var t2 = this;\n    if (void 0 !== this.t) {\n      var n2 = i2.e, r2 = i2.x;\n      if (void 0 !== n2) {\n        n2.x = r2;\n        i2.e = void 0;\n      }\n      if (void 0 !== r2) {\n        r2.e = n2;\n        i2.x = void 0;\n      }\n      if (i2 === this.t) {\n        this.t = r2;\n        if (void 0 === r2) f(function() {\n          var i3;\n          null == (i3 = t2.Z) || i3.call(t2);\n        });\n      }\n    }\n  };\n  l.prototype.subscribe = function(i2) {\n    var t2 = this;\n    return j(function() {\n      var n2 = t2.value;\n      f(function() {\n        return i2(n2);\n      });\n    }, { name: \"sub\" });\n  };\n  l.prototype.valueOf = function() {\n    return this.value;\n  };\n  l.prototype.toString = function() {\n    return this.value + \"\";\n  };\n  l.prototype.toJSON = function() {\n    return this.value;\n  };\n  l.prototype.peek = function() {\n    var i2 = this;\n    return f(function() {\n      return i2.value;\n    });\n  };\n  Object.defineProperty(l.prototype, \"value\", { get: function() {\n    var i2 = a(this);\n    if (void 0 !== i2) i2.i = this.i;\n    return this.v;\n  }, set: function(i2) {\n    if (i2 !== this.v) {\n      if (s > 100) throw new Error(\"Cycle detected\");\n      !(function(i3) {\n        if (0 !== v && 0 === s) {\n          if (i3.l !== e) {\n            i3.l = e;\n            c = { S: i3, v: i3.v, i: i3.i, o: c };\n          }\n        }\n      })(this);\n      this.v = i2;\n      this.i++;\n      d++;\n      v++;\n      try {\n        for (var n2 = this.t; void 0 !== n2; n2 = n2.x) n2.t.N();\n      } finally {\n        t();\n      }\n    }\n  } });\n  function y(i2, t2) {\n    return new l(i2, t2);\n  }\n  function w(i2) {\n    for (var t2 = i2.s; void 0 !== t2; t2 = t2.n) if (t2.S.i !== t2.i || !t2.S.h() || t2.S.i !== t2.i) return true;\n    return false;\n  }\n  function _(i2) {\n    for (var t2 = i2.s; void 0 !== t2; t2 = t2.n) {\n      var n2 = t2.S.n;\n      if (void 0 !== n2) t2.r = n2;\n      t2.S.n = t2;\n      t2.i = -1;\n      if (void 0 === t2.n) {\n        i2.s = t2;\n        break;\n      }\n    }\n  }\n  function b(i2) {\n    var t2 = i2.s, n2 = void 0;\n    while (void 0 !== t2) {\n      var r2 = t2.p;\n      if (-1 === t2.i) {\n        t2.S.U(t2);\n        if (void 0 !== r2) r2.n = t2.n;\n        if (void 0 !== t2.n) t2.n.p = r2;\n      } else n2 = t2;\n      t2.S.n = t2.r;\n      if (void 0 !== t2.r) t2.r = void 0;\n      t2 = r2;\n    }\n    i2.s = n2;\n  }\n  function p(i2, t2) {\n    l.call(this, void 0, t2);\n    this.x = i2;\n    this.s = void 0;\n    this.g = d - 1;\n    this.f = 4;\n  }\n  p.prototype = new l();\n  p.prototype.h = function() {\n    this.f &= -3;\n    if (1 & this.f) return false;\n    if (32 == (36 & this.f)) return true;\n    this.f &= -5;\n    if (this.g === d) return true;\n    this.g = d;\n    this.f |= 1;\n    if (this.i > 0 && !w(this)) {\n      this.f &= -2;\n      return true;\n    }\n    var i2 = o;\n    try {\n      _(this);\n      o = this;\n      var t2 = this.x();\n      if (16 & this.f || this.v !== t2 || 0 === this.i) {\n        this.v = t2;\n        this.f &= -17;\n        this.i++;\n      }\n    } catch (i3) {\n      this.v = i3;\n      this.f |= 16;\n      this.i++;\n    }\n    o = i2;\n    b(this);\n    this.f &= -2;\n    return true;\n  };\n  p.prototype.S = function(i2) {\n    if (void 0 === this.t) {\n      this.f |= 36;\n      for (var t2 = this.s; void 0 !== t2; t2 = t2.n) t2.S.S(t2);\n    }\n    l.prototype.S.call(this, i2);\n  };\n  p.prototype.U = function(i2) {\n    if (void 0 !== this.t) {\n      l.prototype.U.call(this, i2);\n      if (void 0 === this.t) {\n        this.f &= -33;\n        for (var t2 = this.s; void 0 !== t2; t2 = t2.n) t2.S.U(t2);\n      }\n    }\n  };\n  p.prototype.N = function() {\n    if (!(2 & this.f)) {\n      this.f |= 6;\n      for (var i2 = this.t; void 0 !== i2; i2 = i2.x) i2.t.N();\n    }\n  };\n  Object.defineProperty(p.prototype, \"value\", { get: function() {\n    if (1 & this.f) throw new Error(\"Cycle detected\");\n    var i2 = a(this);\n    this.h();\n    if (void 0 !== i2) i2.i = this.i;\n    if (16 & this.f) throw this.v;\n    return this.v;\n  } });\n  function g(i2, t2) {\n    return new p(i2, t2);\n  }\n  function S(i2) {\n    var n2 = i2.m;\n    i2.m = void 0;\n    if (\"function\" == typeof n2) {\n      v++;\n      var r2 = o;\n      o = void 0;\n      try {\n        n2();\n      } catch (t2) {\n        i2.f &= -2;\n        i2.f |= 8;\n        m(i2);\n        throw t2;\n      } finally {\n        o = r2;\n        t();\n      }\n    }\n  }\n  function m(i2) {\n    for (var t2 = i2.s; void 0 !== t2; t2 = t2.n) t2.S.U(t2);\n    i2.x = void 0;\n    i2.s = void 0;\n    S(i2);\n  }\n  function x(i2) {\n    if (o !== this) throw new Error(\"Out-of-order effect\");\n    b(this);\n    o = i2;\n    this.f &= -2;\n    if (8 & this.f) m(this);\n    t();\n  }\n  function E(i2, t2) {\n    this.x = i2;\n    this.m = void 0;\n    this.s = void 0;\n    this.u = void 0;\n    this.f = 32;\n    this.name = null == t2 ? void 0 : t2.name;\n    if (r) r.push(this);\n  }\n  E.prototype.c = function() {\n    var i2 = this.S();\n    try {\n      if (8 & this.f) return;\n      if (void 0 === this.x) return;\n      var t2 = this.x();\n      if (\"function\" == typeof t2) this.m = t2;\n    } finally {\n      i2();\n    }\n  };\n  E.prototype.S = function() {\n    if (1 & this.f) throw new Error(\"Cycle detected\");\n    this.f |= 1;\n    this.f &= -9;\n    S(this);\n    _(this);\n    v++;\n    var i2 = o;\n    o = this;\n    return x.bind(this, i2);\n  };\n  E.prototype.N = function() {\n    if (!(2 & this.f)) {\n      this.f |= 2;\n      this.u = h;\n      h = this;\n    }\n  };\n  E.prototype.d = function() {\n    this.f |= 8;\n    if (!(1 & this.f)) m(this);\n  };\n  E.prototype.dispose = function() {\n    this.d();\n  };\n  function j(i2, t2) {\n    var n2 = new E(i2, t2);\n    try {\n      n2.c();\n    } catch (i3) {\n      n2.d();\n      throw i3;\n    }\n    var r2 = n2.d.bind(n2);\n    r2[Symbol.dispose] = r2;\n    return r2;\n  }\n\n  // node_modules/kerfjs/dist/chunk-3APBEVHF.js\n  function isSignal(value) {\n    return value instanceof l;\n  }\n  function signal(value) {\n    const factory = devHooks.signalFactory;\n    if (factory) return factory(value);\n    return y(value);\n  }\n  function effect(fn) {\n    const wrap = devHooks.wrapEffect;\n    return j(wrap ? wrap(fn) : fn);\n  }\n\n  // node_modules/kerfjs/dist/chunk-GY4XV2UV.js\n  var LIST_MARKER_PREFIX = \"kf-list:\";\n  function flatten(segment, withMarkers) {\n    if (segment.kind === \"static\") return segment.html;\n    if (segment.kind === \"list\") {\n      const items = segment.items.map((i2) => i2.html).join(\"\");\n      return withMarkers ? `<!--${LIST_MARKER_PREFIX}${segment.id}-->${items}` : items;\n    }\n    return segment.parts.map((p2) => flatten(p2, withMarkers)).join(\"\");\n  }\n  function flattenWithoutListItems(segment) {\n    if (segment.kind === \"static\") return segment.html;\n    if (segment.kind === \"list\") return `<!--${LIST_MARKER_PREFIX}${segment.id}-->`;\n    return segment.parts.map(flattenWithoutListItems).join(\"\");\n  }\n  function collectLists(segment, out = /* @__PURE__ */ new Map()) {\n    if (segment.kind === \"list\") out.set(segment.id, segment);\n    else if (segment.kind === \"mixed\") {\n      for (const part of segment.parts) collectLists(part, out);\n    }\n    return out;\n  }\n  function mergeChildSegments(parts) {\n    if (parts.length === 0) return { kind: \"static\", html: \"\" };\n    if (parts.every((p2) => p2.kind === \"static\")) {\n      return {\n        kind: \"static\",\n        html: parts.map((p2) => p2.html).join(\"\")\n      };\n    }\n    const merged = [];\n    let coalesced = \"\";\n    for (const p2 of parts) {\n      if (p2.kind === \"static\") {\n        coalesced += p2.html;\n      } else {\n        if (coalesced !== \"\") {\n          merged.push({ kind: \"static\", html: coalesced });\n          coalesced = \"\";\n        }\n        merged.push(p2);\n      }\n    }\n    if (coalesced !== \"\") merged.push({ kind: \"static\", html: coalesced });\n    return { kind: \"mixed\", parts: merged };\n  }\n  function wrapWithTags(child, openTag, closeTag) {\n    if (child.kind === \"static\") {\n      return { kind: \"static\", html: openTag + child.html + closeTag };\n    }\n    if (child.kind === \"mixed\") {\n      return {\n        kind: \"mixed\",\n        parts: [\n          { kind: \"static\", html: openTag },\n          ...child.parts,\n          { kind: \"static\", html: closeTag }\n        ]\n      };\n    }\n    return {\n      kind: \"mixed\",\n      parts: [\n        { kind: \"static\", html: openTag },\n        child,\n        { kind: \"static\", html: closeTag }\n      ]\n    };\n  }\n\n  // node_modules/kerfjs/dist/chunk-SUPUPSBE.js\n  function syncFormProp(el, name, value, present) {\n    const tag = el.tagName;\n    if (name === \"checked\") {\n      if (tag === \"INPUT\") el.checked = present;\n    } else if (name === \"value\") {\n      if (tag === \"INPUT\" && el !== document.activeElement) {\n        el.value = present ? value : \"\";\n      }\n    } else if (name === \"selected\") {\n      if (tag === \"OPTION\") el.selected = present;\n    }\n  }\n  var URL_ATTRS = /* @__PURE__ */ new Set([\"href\", \"src\", \"xlink:href\", \"formaction\", \"action\", \"data\"]);\n  var DANGEROUS_SCHEMES = /* @__PURE__ */ new Set([\"javascript\", \"vbscript\"]);\n  var INERT_JAVASCRIPT_URLS = /* @__PURE__ */ new Set([\n    \"javascript:\",\n    \"javascript:;\",\n    \"javascript:void(0)\",\n    \"javascript:void(0);\",\n    \"javascript:void 0\",\n    \"javascript:void 0;\"\n  ]);\n  var CONTROL_CHARS = /[\\u0000-\\u001F\\u007F]/g;\n  function normalizeUrl(value) {\n    return value.replace(CONTROL_CHARS, \"\").replace(/^\\s+/, \"\");\n  }\n  function schemeOf(normalized) {\n    const m2 = /^([a-zA-Z][a-zA-Z0-9+.-]*):/.exec(normalized);\n    return m2 ? m2[1].toLowerCase() : null;\n  }\n  function isDangerousDataUrl(normalized) {\n    const media = /^data:([^;,]*)/.exec(normalized.toLowerCase())?.[1].trim() ?? \"\";\n    if (media === \"\" || media === \"text/plain\" || media === \"text/css\") return false;\n    if (media === \"image/svg+xml\") return true;\n    if (media.startsWith(\"image/\")) return false;\n    if (media.startsWith(\"font/\") || media.startsWith(\"application/font\")) return false;\n    if (media.startsWith(\"audio/\") || media.startsWith(\"video/\")) return false;\n    return true;\n  }\n  function isDangerousUrlValue(name, value) {\n    if (!URL_ATTRS.has(name)) return false;\n    const normalized = normalizeUrl(value);\n    const scheme = schemeOf(normalized);\n    if (scheme === null) return false;\n    if (DANGEROUS_SCHEMES.has(scheme)) {\n      return !INERT_JAVASCRIPT_URLS.has(normalized.trimEnd().toLowerCase());\n    }\n    return scheme === \"data\" && isDangerousDataUrl(normalized);\n  }\n  function dangerousUrlWarning(name, value) {\n    return `dropped dangerous URL value for ${name}=${JSON.stringify(value.slice(0, 80))}. kerf blocks javascript:, vbscript:, and script-executing data: URLs (text/html, image/svg+xml, xml) in href/src/data/formaction/action/xlink:href by default. Wrap in raw() if this is intentional (e.g. bookmarklets), or sanitize upstream.`;\n  }\n  function reportDangerousUrl(context22, name, value) {\n    const message2 = `${context22}: ${dangerousUrlWarning(name, value)}`;\n    devHooks.urlScreenThrow?.(message2);\n    console.warn(message2);\n  }\n  var BIND_ATTR = \"data-kfb\";\n  var TEXT_MARKER_PREFIX = \"kfb:\";\n  var BIND_ATTR_ROW = \"data-kfbrow\";\n  var ROW_TEXT_PREFIX = \"kfbr:\";\n  var context = null;\n  var rowSink = null;\n  var rowCounter = 0;\n  var NO_DISPOSERS = Object.freeze([]);\n  function newBindingContext() {\n    return { counter: 0, list: [] };\n  }\n  function _setBindingContext(c2) {\n    context = c2;\n  }\n  function bindAttr(attr, signal2) {\n    if (rowSink !== null) {\n      const id = `a${rowCounter++}`;\n      rowSink.push({ kind: \"attr\", id, attr, signal: signal2 });\n      return id;\n    }\n    if (context !== null) {\n      const id = `a${context.counter++}`;\n      context.list.push({ kind: \"attr\", id, attr, signal: signal2 });\n      return id;\n    }\n    return null;\n  }\n  function bindMarkerAttr() {\n    return rowSink !== null ? BIND_ATTR_ROW : BIND_ATTR;\n  }\n  function bindText(signal2) {\n    if (rowSink !== null) {\n      const id = `t${rowCounter++}`;\n      rowSink.push({ kind: \"text\", id, signal: signal2 });\n      return `<!--${ROW_TEXT_PREFIX}${id}-->`;\n    }\n    if (context !== null) {\n      const id = `t${context.counter++}`;\n      context.list.push({ kind: \"text\", id, signal: signal2 });\n      return `<!--${TEXT_MARKER_PREFIX}${id}-->`;\n    }\n    return null;\n  }\n  function wireBindings(rootEl, ctx, prevDisposers) {\n    for (const d2 of prevDisposers) d2();\n    if (ctx.list.length === 0) return NO_DISPOSERS;\n    const disposers = [];\n    wireInto(rootEl, ctx.list, disposers);\n    return disposers;\n  }\n  function wireRowBindings(rowNode, bindings) {\n    const disposers = new Array(bindings.length);\n    const rootIds = rowNode.getAttribute(BIND_ATTR_ROW);\n    let rootIdSet = null;\n    let descIndex = null;\n    let textMarkers = null;\n    for (let i2 = 0; i2 < bindings.length; i2++) {\n      const b2 = bindings[i2];\n      if (b2.kind === \"attr\") {\n        let onRoot = false;\n        if (rootIds !== null) {\n          if (rootIds === b2.id) {\n            onRoot = true;\n          } else if (rootIds.indexOf(\",\") !== -1) {\n            rootIdSet ??= new Set(rootIds.split(\",\"));\n            onRoot = rootIdSet.has(b2.id);\n          }\n        }\n        let el;\n        if (onRoot) {\n          el = rowNode;\n        } else {\n          descIndex ??= indexAttrEls(rowNode, BIND_ATTR_ROW);\n          el = descIndex.get(b2.id);\n        }\n        if (el === void 0) continue;\n        disposers[i2] = attachAttrEffect(el, b2.attr, b2.signal);\n      } else {\n        if (textMarkers === null) {\n          textMarkers = /* @__PURE__ */ new Map();\n          collectComments(rowNode, ROW_TEXT_PREFIX, textMarkers);\n        }\n        const marker = textMarkers.get(b2.id);\n        if (marker === void 0) continue;\n        disposers[i2] = attachTextEffect(marker, b2.signal);\n      }\n    }\n    return disposers;\n  }\n  function disposeRowBindings(disposers) {\n    if (disposers === void 0) return;\n    for (const d2 of disposers) d2();\n  }\n  function carryOrRewireRowBindings(node, oldBindings, oldDisposers, newBindings) {\n    const oldLen = oldBindings === void 0 ? 0 : oldBindings.length;\n    const newLen = newBindings === void 0 ? 0 : newBindings.length;\n    if (oldLen === newLen) {\n      let same = true;\n      for (let i2 = 0; i2 < newLen; i2++) {\n        if (oldBindings[i2].signal !== newBindings[i2].signal) {\n          same = false;\n          break;\n        }\n      }\n      if (same) return { bindings: oldBindings, bindingDisposers: oldDisposers };\n    }\n    disposeRowBindings(oldDisposers);\n    return {\n      bindings: newBindings,\n      bindingDisposers: newLen > 0 ? wireRowBindings(node, newBindings) : void 0\n    };\n  }\n  function wireInto(scope, bindings, disposers) {\n    const attrEls = indexAttrEls(scope, BIND_ATTR);\n    const textMarkers = /* @__PURE__ */ new Map();\n    collectComments(scope, TEXT_MARKER_PREFIX, textMarkers);\n    for (const b2 of bindings) {\n      if (b2.kind === \"attr\") {\n        const el = attrEls.get(b2.id);\n        if (el === void 0) continue;\n        disposers.push(attachAttrEffect(el, b2.attr, b2.signal));\n      } else {\n        const marker = textMarkers.get(b2.id);\n        if (marker === void 0) continue;\n        disposers.push(attachTextEffect(marker, b2.signal));\n      }\n    }\n  }\n  function indexAttrEls(scope, attrName) {\n    const map = /* @__PURE__ */ new Map();\n    for (const el of scope.querySelectorAll(`[${attrName}]`)) {\n      for (const id of el.getAttribute(attrName).split(\",\")) map.set(id, el);\n    }\n    return map;\n  }\n  function attachAttrEffect(el, attr, signal2) {\n    return effect(() => setBoundAttr(el, attr, signal2.value));\n  }\n  var insertedTextNodes = /* @__PURE__ */ new WeakMap();\n  function boundTextNodeOf(marker) {\n    const t2 = insertedTextNodes.get(marker);\n    return t2 !== void 0 && marker.nextSibling === t2 ? t2 : null;\n  }\n  function attachTextEffect(marker, signal2) {\n    let text = insertedTextNodes.get(marker);\n    if (text === void 0 || marker.nextSibling !== text) {\n      text = marker.ownerDocument.createTextNode(\"\");\n      marker.parentNode.insertBefore(text, marker.nextSibling);\n      insertedTextNodes.set(marker, text);\n    }\n    const node = text;\n    return effect(() => {\n      node.data = coerceText(signal2.value);\n    });\n  }\n  function setBoundAttr(el, name, value) {\n    if (value == null || value === false) {\n      el.removeAttribute(name);\n      syncFormProp(el, name, \"\", false);\n      return;\n    }\n    if (value === true) {\n      el.setAttribute(name, \"\");\n      syncFormProp(el, name, \"\", true);\n      return;\n    }\n    if (isSafeHtmlValue(value)) {\n      el.setAttribute(name, value.__html);\n      syncFormProp(el, name, value.__html, true);\n      return;\n    }\n    const str = String(value);\n    if (isDangerousUrlValue(name, str)) {\n      reportDangerousUrl(\"kerf binding\", name, str);\n      el.removeAttribute(name);\n      return;\n    }\n    el.setAttribute(name, str);\n    syncFormProp(el, name, str, true);\n  }\n  var SAFE_HTML_BRAND = /* @__PURE__ */ Symbol.for(\"kerfjs.SafeHtml\");\n  function isSafeHtmlValue(v2) {\n    return typeof v2 === \"object\" && v2 !== null && v2[SAFE_HTML_BRAND] === true;\n  }\n  function coerceText(value) {\n    if (value == null || typeof value === \"boolean\") return \"\";\n    return String(value);\n  }\n  function collectComments(node, prefix, out) {\n    for (let c2 = node.firstChild; c2 !== null; c2 = c2.nextSibling) {\n      if (c2.nodeType === Node.COMMENT_NODE) {\n        const data = c2.data;\n        if (data.startsWith(prefix)) out.set(data.slice(prefix.length), c2);\n      } else if (c2.nodeType === Node.ELEMENT_NODE) {\n        collectComments(c2, prefix, out);\n      }\n    }\n  }\n  function escapeHtml(str) {\n    return str.replace(/&/g, \"&amp;\").replace(/</g, \"&lt;\").replace(/>/g, \"&gt;\").replace(/\"/g, \"&quot;\");\n  }\n  function escapeAttr(str) {\n    return str.replace(/&/g, \"&amp;\").replace(/\"/g, \"&quot;\").replace(/'/g, \"&#39;\").replace(/</g, \"&lt;\").replace(/>/g, \"&gt;\");\n  }\n  var ATTR_ALIASES = {\n    // HTML attributes\n    className: \"class\",\n    htmlFor: \"for\",\n    httpEquiv: \"http-equiv\",\n    acceptCharset: \"accept-charset\",\n    accessKey: \"accesskey\",\n    autoCapitalize: \"autocapitalize\",\n    autoComplete: \"autocomplete\",\n    autoFocus: \"autofocus\",\n    autoPlay: \"autoplay\",\n    colSpan: \"colspan\",\n    contentEditable: \"contenteditable\",\n    crossOrigin: \"crossorigin\",\n    dateTime: \"datetime\",\n    defaultChecked: \"checked\",\n    defaultSelected: \"selected\",\n    defaultValue: \"value\",\n    encType: \"enctype\",\n    formAction: \"formaction\",\n    formEncType: \"formenctype\",\n    formMethod: \"formmethod\",\n    formNoValidate: \"formnovalidate\",\n    formTarget: \"formtarget\",\n    hrefLang: \"hreflang\",\n    inputMode: \"inputmode\",\n    maxLength: \"maxlength\",\n    minLength: \"minlength\",\n    noModule: \"nomodule\",\n    noValidate: \"novalidate\",\n    readOnly: \"readonly\",\n    referrerPolicy: \"referrerpolicy\",\n    rowSpan: \"rowspan\",\n    spellCheck: \"spellcheck\",\n    srcDoc: \"srcdoc\",\n    srcLang: \"srclang\",\n    srcSet: \"srcset\",\n    tabIndex: \"tabindex\",\n    useMap: \"usemap\",\n    // SVG presentation attributes (camelCase → kebab-case)\n    strokeWidth: \"stroke-width\",\n    strokeLinecap: \"stroke-linecap\",\n    strokeLinejoin: \"stroke-linejoin\",\n    strokeDasharray: \"stroke-dasharray\",\n    strokeDashoffset: \"stroke-dashoffset\",\n    strokeMiterlimit: \"stroke-miterlimit\",\n    strokeOpacity: \"stroke-opacity\",\n    fillOpacity: \"fill-opacity\",\n    fillRule: \"fill-rule\",\n    clipPath: \"clip-path\",\n    clipRule: \"clip-rule\",\n    colorInterpolation: \"color-interpolation\",\n    colorInterpolationFilters: \"color-interpolation-filters\",\n    floodColor: \"flood-color\",\n    floodOpacity: \"flood-opacity\",\n    lightingColor: \"lighting-color\",\n    stopColor: \"stop-color\",\n    stopOpacity: \"stop-opacity\",\n    shapeRendering: \"shape-rendering\",\n    imageRendering: \"image-rendering\",\n    textRendering: \"text-rendering\",\n    pointerEvents: \"pointer-events\",\n    vectorEffect: \"vector-effect\",\n    paintOrder: \"paint-order\",\n    // SVG text/font attributes\n    fontFamily: \"font-family\",\n    fontSize: \"font-size\",\n    fontStyle: \"font-style\",\n    fontVariant: \"font-variant\",\n    fontWeight: \"font-weight\",\n    fontStretch: \"font-stretch\",\n    textAnchor: \"text-anchor\",\n    textDecoration: \"text-decoration\",\n    dominantBaseline: \"dominant-baseline\",\n    alignmentBaseline: \"alignment-baseline\",\n    baselineShift: \"baseline-shift\",\n    letterSpacing: \"letter-spacing\",\n    wordSpacing: \"word-spacing\",\n    writingMode: \"writing-mode\",\n    // SVG marker attributes\n    markerStart: \"marker-start\",\n    markerMid: \"marker-mid\",\n    markerEnd: \"marker-end\",\n    // SVG xlink (legacy but still used)\n    xlinkHref: \"xlink:href\",\n    xlinkShow: \"xlink:show\",\n    xlinkActuate: \"xlink:actuate\",\n    xlinkType: \"xlink:type\",\n    xlinkRole: \"xlink:role\",\n    xlinkTitle: \"xlink:title\",\n    xlinkArcrole: \"xlink:arcrole\",\n    xmlBase: \"xml:base\",\n    xmlLang: \"xml:lang\",\n    xmlSpace: \"xml:space\",\n    xmlnsXlink: \"xmlns:xlink\"\n  };\n  var SAFE_HTML_BRAND2 = /* @__PURE__ */ Symbol.for(\"kerfjs.SafeHtml\");\n  var SafeHtml = class {\n    __html;\n    __segment;\n    // Branded so `isSafeHtml()` recognizes instances from any copy of this module.\n    [SAFE_HTML_BRAND2] = true;\n    constructor(input) {\n      if (typeof input === \"string\") {\n        this.__segment = { kind: \"static\", html: input };\n        this.__html = input;\n      } else {\n        this.__segment = input;\n        this.__html = flatten(input, false);\n      }\n    }\n    toString() {\n      return this.__html;\n    }\n  };\n  function isSafeHtml(value) {\n    return typeof value === \"object\" && value !== null && value[SAFE_HTML_BRAND2] === true;\n  }\n  var VOID_TAGS = /* @__PURE__ */ new Set([\n    \"area\",\n    \"base\",\n    \"br\",\n    \"col\",\n    \"embed\",\n    \"hr\",\n    \"img\",\n    \"input\",\n    \"link\",\n    \"meta\",\n    \"source\",\n    \"track\",\n    \"wbr\"\n  ]);\n  function toSegment(child) {\n    if (child == null || typeof child === \"boolean\") return { kind: \"static\", html: \"\" };\n    if (isSignal(child)) {\n      const marker = bindText(child);\n      if (marker !== null) return { kind: \"static\", html: marker };\n      const v2 = child.value;\n      return { kind: \"static\", html: v2 == null || typeof v2 === \"boolean\" ? \"\" : escapeHtml(String(v2)) };\n    }\n    if (isSafeHtml(child)) {\n      return child.__segment ?? { kind: \"static\", html: child.__html };\n    }\n    if (typeof child === \"string\") return { kind: \"static\", html: escapeHtml(child) };\n    if (typeof child === \"number\") return { kind: \"static\", html: String(child) };\n    if (Array.isArray(child)) return mergeChildSegments(child.map(toSegment));\n    const maybeNode = child;\n    if (typeof maybeNode === \"object\" && maybeNode !== null && (\"nodeType\" in maybeNode || \"outerHTML\" in maybeNode)) {\n      throw new Error(\n        \"JSX: DOM elements cannot be passed as children (the JSX runtime renders to HTML strings). Build the tree in one JSX expression and use querySelector after toElement() to get element refs.\"\n      );\n    }\n    throw new Error(\n      `JSX: unsupported child of type ${describeValue(child)}. Children must be SafeHtml, string, number, boolean, null, undefined, or an array of those. Common mistakes: passing a Signal/Store object directly (use signal.value or store.state.value), passing a function (call it first), or passing a Promise (await it before render).`\n    );\n  }\n  function describeValue(v2) {\n    if (Array.isArray(v2)) return \"array\";\n    if (typeof v2 === \"object\" && v2 !== null) {\n      const ctor = v2.constructor?.name;\n      return ctor && ctor !== \"Object\" ? `object (${ctor})` : \"object\";\n    }\n    return typeof v2;\n  }\n  var SAFE_ATTR_NAME = /^[A-Za-z_:][\\w.:-]*$/;\n  function assertEmittableAttrName(key, name, isFn) {\n    if (/^on[a-z]/i.test(name)) {\n      if (isFn) {\n        throw new Error(\n          `JSX: inline event handlers like ${key}={fn} are not supported by kerf's JSX \\u2192 HTML-string runtime. Use event delegation from the mount root instead:\n\n  delegate(rootEl, 'click', '[data-action=\"...\"]', (evt, target) => { ... });\n  <button data-action=\"...\">click</button>\n\nSee docs/5-event-delegation.md for the tier-1/tier-2/tier-3 model.`\n        );\n      }\n      throw new Error(\n        `JSX: event-handler attribute ${JSON.stringify(key)} is not allowed \\u2014 an \\`on*\\` attribute (whether a string emitted into HTML or a signal bound via setAttribute) installs a live inline handler, an XSS vector. kerf uses event delegation: delegate(rootEl, 'click', '[data-action=\"...\"]', handler). See docs/5-event-delegation.md.`\n      );\n    }\n    if (!SAFE_ATTR_NAME.test(name)) {\n      throw new Error(\n        `JSX: invalid attribute name ${JSON.stringify(key)}. Attribute names must be a letter/underscore/colon followed by letters, digits, or \"_.:-\" (e.g. class, data-id, aria-label, xlink:href). This usually means an untrusted object was spread into JSX ({...obj}) with attacker-controlled keys \\u2014 validate keys first.`\n      );\n    }\n  }\n  function renderAttr(key, value) {\n    return renderAttrNamed(key, ATTR_ALIASES[key] ?? key, value);\n  }\n  function renderAttrNamed(key, name, value) {\n    if (value == null || value === false) return \"\";\n    assertEmittableAttrName(key, name, typeof value === \"function\");\n    if (value === true) return ` ${name}`;\n    let strValue;\n    if (isSafeHtml(value)) {\n      strValue = value.__html;\n    } else if (typeof value === \"number\") {\n      strValue = String(value);\n    } else if (typeof value === \"string\") {\n      if (isDangerousUrlValue(name, value)) {\n        reportDangerousUrl(\"JSX\", name, value);\n        return \"\";\n      }\n      strValue = escapeAttr(value);\n    } else {\n      throw new Error(\n        `JSX: unsupported value for attribute \"${key}\" \\u2014 got ${describeValue(value)}. Attribute values must be string, number, boolean, null, undefined, or SafeHtml. Did you mean to read .value off a Signal, or stringify the object first?`\n      );\n    }\n    return ` ${name}=\"${strValue}\"`;\n  }\n  function jsx(tag, props) {\n    if (typeof tag === \"function\") return tag(props);\n    const { children, ...attrs } = props;\n    let attrStr = \"\";\n    let bindIds = null;\n    for (const [k, v2] of Object.entries(attrs)) {\n      if (isSignal(v2)) {\n        const name = ATTR_ALIASES[k] ?? k;\n        assertEmittableAttrName(k, name, false);\n        const id = bindAttr(name, v2);\n        if (id !== null) {\n          (bindIds ??= []).push(id);\n          continue;\n        }\n        attrStr += renderAttr(k, v2.value);\n        continue;\n      }\n      attrStr += renderAttr(k, v2);\n    }\n    if (bindIds !== null) attrStr += ` ${bindMarkerAttr()}=\"${bindIds.join(\",\")}\"`;\n    if (VOID_TAGS.has(tag)) return new SafeHtml(`<${tag}${attrStr}>`);\n    const childSegment = children != null ? toSegment(children) : { kind: \"static\", html: \"\" };\n    return new SafeHtml(wrapWithTags(childSegment, `<${tag}${attrStr}>`, `</${tag}>`));\n  }\n  function Fragment({ children }) {\n    return new SafeHtml(children != null ? toSegment(children) : { kind: \"static\", html: \"\" });\n  }\n\n  // node_modules/kerfjs/dist/chunk-SRWQKB33.js\n  var context2 = null;\n  function _setRenderContext(c2) {\n    context2 = c2;\n  }\n  function _resetCallOrderListState(ctx) {\n    const isCallOrderId = (id) => !id.startsWith(\"k:\");\n    for (const map of [ctx.caches, ctx.bindingCounts, ctx.bindingSources]) {\n      for (const id of Array.from(map.keys())) {\n        if (isCallOrderId(id)) map.delete(id);\n      }\n    }\n  }\n  function moveNode(parent, node, ref) {\n    const move = parent.moveBefore;\n    if (move !== void 0 && node.isConnected) {\n      move.call(parent, node, ref);\n    } else {\n      parent.insertBefore(node, ref);\n    }\n  }\n  function captureFocus(liveParent) {\n    const active = document.activeElement;\n    if (active === null || active === document.body) return null;\n    if (!liveParent.contains(active)) return null;\n    const el = active;\n    let selStart = null;\n    let selEnd = null;\n    if (el.tagName === \"INPUT\" || el.tagName === \"TEXTAREA\") {\n      try {\n        selStart = el.selectionStart;\n        selEnd = el.selectionEnd;\n      } catch {\n      }\n    }\n    return { el, selStart, selEnd };\n  }\n  function restoreFocus(snap) {\n    if (document.activeElement === snap.el) return;\n    if (!snap.el.isConnected) return;\n    snap.el.focus();\n    if (snap.selStart !== null && snap.selEnd !== null) {\n      try {\n        snap.el.setSelectionRange(snap.selStart, snap.selEnd);\n      } catch {\n      }\n    }\n  }\n  var ID_KEY_PREFIX = \"id:\";\n  var DATA_KEY_PREFIX = \"data-key:\";\n  var ELEMENT_NODE = 1;\n  var TEXT_NODE = 3;\n  var COMMENT_NODE = 8;\n  function getNodeKey(node) {\n    if (node.nodeType !== ELEMENT_NODE) return void 0;\n    const el = node;\n    if (el.id !== \"\") return `${ID_KEY_PREFIX}${el.id}`;\n    if (el.dataset !== void 0 && el.dataset.key !== void 0) {\n      return `${DATA_KEY_PREFIX}${el.dataset.key}`;\n    }\n    return void 0;\n  }\n  var EMPTY_OWNED = /* @__PURE__ */ new Set();\n  function morph(liveRoot, template, ownedItems = EMPTY_OWNED) {\n    if (liveRoot == null) {\n      throw new Error(\n        'morph: liveRoot is null/undefined \\u2014 pass the live element, e.g. morph(document.getElementById(\"app\")!, template). A common cause is a typo in the id or selector that returns null at runtime even though the TypeScript types say Element.'\n      );\n    }\n    const templateEl = isElementNode(template) ? template : parseTemplate(liveRoot, template);\n    const focusSnap = captureFocus(liveRoot);\n    morphChildren(liveRoot, templateEl, ownedItems);\n    if (focusSnap !== null) restoreFocus(focusSnap);\n  }\n  function _morphElement(fromEl, toEl, ownedItems = EMPTY_OWNED) {\n    morphElement(fromEl, toEl, ownedItems);\n  }\n  function isElementNode(t2) {\n    return typeof t2 === \"object\" && t2 !== null && t2.nodeType === ELEMENT_NODE;\n  }\n  function parseTemplate(liveRoot, template) {\n    const el = liveRoot.cloneNode(false);\n    el.innerHTML = String(template);\n    return el;\n  }\n  function protectionTag(node) {\n    const { dataset } = node;\n    return (dataset.morphSkip !== void 0 ? \"s\" : \"\") + (dataset.morphSkipChildren !== void 0 ? \"c\" : \"\") + (dataset.morphPreserve !== void 0 ? \"p\" : \"\");\n  }\n  var MARKER_PREFIXES = [LIST_MARKER_PREFIX, TEXT_MARKER_PREFIX, ROW_TEXT_PREFIX];\n  function isMarker(node) {\n    if (node.nodeType !== COMMENT_NODE) return false;\n    const { data } = node;\n    return MARKER_PREFIXES.some((prefix) => data.startsWith(prefix));\n  }\n  function markersPairable(a2, b2) {\n    if (!isMarker(a2) && !isMarker(b2)) return true;\n    return a2.data === b2.data;\n  }\n  function skipOwned(node, ownedItems) {\n    while (node !== null && node.nodeType === ELEMENT_NODE && ownedItems.has(node)) {\n      node = node.nextSibling;\n    }\n    return node;\n  }\n  function isListMarker(node) {\n    return node.nodeType === COMMENT_NODE && node.data.startsWith(LIST_MARKER_PREFIX);\n  }\n  function afterListRegion(marker, ownedItems) {\n    let last = marker;\n    for (let r2 = marker.nextSibling; r2 !== null; r2 = r2.nextSibling) {\n      if (isListMarker(r2)) break;\n      if (r2.nodeType === ELEMENT_NODE && ownedItems.has(r2)) last = r2;\n    }\n    return last.nextSibling;\n  }\n  function morphChildren(fromParent, toParent, ownedItems) {\n    const keyed = /* @__PURE__ */ new Map();\n    for (let c2 = fromParent.firstChild; c2 !== null; c2 = c2.nextSibling) {\n      if (c2.nodeType === ELEMENT_NODE && ownedItems.has(c2)) continue;\n      const k = getNodeKey(c2);\n      if (k !== void 0) keyed.set(k, c2);\n    }\n    let fromChild = skipOwned(fromParent.firstChild, ownedItems);\n    let toChild = toParent.firstChild;\n    while (toChild !== null) {\n      const toNext = toChild.nextSibling;\n      let matched = null;\n      const toKey = getNodeKey(toChild);\n      if (toKey !== void 0 && keyed.has(toKey)) {\n        matched = keyed.get(toKey);\n        keyed.delete(toKey);\n        if (matched !== fromChild) {\n          moveNode(fromParent, matched, fromChild);\n        } else {\n          fromChild = skipOwned(fromChild.nextSibling, ownedItems);\n        }\n      }\n      if (matched === null && fromChild !== null && fromChild.nodeType === toChild.nodeType && markersPairable(fromChild, toChild) && (toChild.nodeType !== ELEMENT_NODE || fromChild.tagName === toChild.tagName && getNodeKey(fromChild) === void 0 && toKey === void 0 && protectionTag(fromChild) === protectionTag(toChild))) {\n        matched = fromChild;\n        fromChild = skipOwned(\n          isListMarker(matched) ? afterListRegion(matched, ownedItems) : fromChild.nextSibling,\n          ownedItems\n        );\n        if (matched.nodeType === COMMENT_NODE && fromChild !== null) {\n          const owned = boundTextNodeOf(matched);\n          if (owned !== null && fromChild === owned) {\n            fromChild = skipOwned(owned.nextSibling, ownedItems);\n          }\n        }\n      }\n      if (matched === null && toChild.nodeType === ELEMENT_NODE && fromChild !== null && toKey === void 0) {\n        const toTag = toChild.tagName;\n        for (let scan = fromChild.nextSibling; scan !== null; scan = scan.nextSibling) {\n          if (scan.nodeType !== ELEMENT_NODE) continue;\n          const el = scan;\n          if (ownedItems.has(el)) continue;\n          if (el.tagName !== toTag || getNodeKey(el) !== void 0) continue;\n          if (protectionTag(el) !== protectionTag(toChild)) continue;\n          matched = el;\n          moveNode(fromParent, el, fromChild);\n          break;\n        }\n      }\n      if (matched === null && fromChild !== null && toChild.nodeType === COMMENT_NODE && toChild.data.startsWith(LIST_MARKER_PREFIX)) {\n        const wantData = toChild.data;\n        for (let scan = fromChild.nextSibling; scan !== null; scan = scan.nextSibling) {\n          if (scan.nodeType !== COMMENT_NODE || scan.data !== wantData) continue;\n          const regionEnd = afterListRegion(scan, ownedItems);\n          const run2 = [];\n          for (let r2 = scan; r2 !== null && r2 !== regionEnd; r2 = r2.nextSibling) {\n            run2.push(r2);\n          }\n          const focusSnap = captureFocus(fromParent);\n          for (const node of run2) moveNode(fromParent, node, fromChild);\n          if (focusSnap !== null) restoreFocus(focusSnap);\n          matched = scan;\n          break;\n        }\n      }\n      if (matched !== null) {\n        morphNode(matched, toChild, ownedItems);\n      } else {\n        const cloned = toChild.cloneNode(true);\n        fromParent.insertBefore(cloned, fromChild);\n      }\n      toChild = toNext;\n    }\n    while (fromChild !== null) {\n      const next = fromChild.nextSibling;\n      if (fromChild.nodeType === ELEMENT_NODE) {\n        const el = fromChild;\n        if (!ownedItems.has(el) && el.dataset.morphPreserve === void 0) {\n          fromParent.removeChild(fromChild);\n        }\n      } else {\n        fromParent.removeChild(fromChild);\n      }\n      fromChild = next;\n    }\n  }\n  function morphNode(fromNode, toNode, ownedItems) {\n    if (fromNode.nodeType === ELEMENT_NODE) {\n      morphElement(fromNode, toNode, ownedItems);\n      return;\n    }\n    if (fromNode.nodeType === TEXT_NODE || fromNode.nodeType === COMMENT_NODE) {\n      const fromText = fromNode;\n      const toText = toNode;\n      if (fromText.data !== toText.data) fromText.data = toText.data;\n    }\n  }\n  function morphElement(fromEl, toEl, ownedItems) {\n    if (fromEl.tagName !== toEl.tagName) {\n      const replacement = toEl.cloneNode(true);\n      fromEl.parentNode?.replaceChild(replacement, fromEl);\n      return;\n    }\n    if (fromEl.dataset.morphSkip !== void 0) return;\n    if (fromEl.isEqualNode(toEl)) return;\n    if (fromEl === document.activeElement) {\n      const ce = fromEl.getAttribute(\"contenteditable\");\n      if (ce !== null && ce.toLowerCase() !== \"false\") return;\n      if (isTextInputOrTextarea(fromEl)) preserveTextEntryState(fromEl, toEl);\n    }\n    morphAttributes(fromEl, toEl);\n    if (fromEl.dataset.morphSkipChildren !== void 0) return;\n    const syncTextareaValue = fromEl.tagName === \"TEXTAREA\" && fromEl !== document.activeElement && fromEl.textContent !== toEl.textContent;\n    morphChildren(fromEl, toEl, ownedItems);\n    if (syncTextareaValue) {\n      fromEl.value = toEl.textContent;\n    }\n  }\n  function isUserAgentOwnedAttr(tagName, name) {\n    return name === \"open\" && (tagName === \"DETAILS\" || tagName === \"DIALOG\");\n  }\n  function morphAttributes(fromEl, toEl) {\n    const toAttrs = toEl.attributes;\n    for (let i2 = 0; i2 < toAttrs.length; i2++) {\n      const attr = toAttrs[i2];\n      const ns = attr.namespaceURI;\n      const name = attr.localName;\n      const value = attr.value;\n      if (ns !== null) {\n        if (fromEl.getAttributeNS(ns, name) !== value) {\n          fromEl.setAttributeNS(ns, attr.name, value);\n        }\n      } else if (fromEl.getAttribute(name) !== value) {\n        fromEl.setAttribute(name, value);\n        syncFormProp(fromEl, name, value, true);\n      }\n    }\n    const fromAttrs = fromEl.attributes;\n    const fromTag = fromEl.tagName;\n    for (let i2 = fromAttrs.length - 1; i2 >= 0; i2--) {\n      const attr = fromAttrs[i2];\n      const ns = attr.namespaceURI;\n      const name = attr.localName;\n      if (ns !== null) {\n        if (!toEl.hasAttributeNS(ns, name)) fromEl.removeAttributeNS(ns, name);\n      } else if (!toEl.hasAttribute(name) && !isUserAgentOwnedAttr(fromTag, name)) {\n        fromEl.removeAttribute(name);\n        syncFormProp(fromEl, name, \"\", false);\n      }\n    }\n  }\n  function isTextInputOrTextarea(el) {\n    if (el.tagName === \"TEXTAREA\") return true;\n    if (el.tagName === \"INPUT\") {\n      const type = el.type;\n      return type === \"text\" || type === \"search\" || type === \"url\" || type === \"email\" || type === \"tel\" || type === \"password\" || type === \"\";\n    }\n    return false;\n  }\n  function preserveTextEntryState(fromEl, toEl) {\n    if (fromEl.tagName === \"TEXTAREA\" || fromEl.tagName === \"INPUT\") {\n      const fromInput = fromEl;\n      const toInput = toEl;\n      toInput.value = fromInput.value;\n      try {\n        toInput.setSelectionRange(fromInput.selectionStart, fromInput.selectionEnd);\n      } catch {\n      }\n    }\n  }\n  function endAnchor(binding) {\n    if (binding.items.length > 0) {\n      return binding.items[binding.items.length - 1].node.nextSibling;\n    }\n    return binding.marker.nextSibling;\n  }\n  var LT = 60;\n  var GT = 62;\n  var DQUOTE = 34;\n  var SQUOTE = 39;\n  var AMP = 38;\n  var EQ = 61;\n  var SLASH = 47;\n  var TEXT_NODE2 = 3;\n  var ELEMENT_NODE2 = 1;\n  function isWhitespace(cc) {\n    return cc === 32 || cc === 9 || cc === 10 || cc === 13;\n  }\n  function tryAttributeOnlyFastPath(liveNode, oldHtml, newHtml) {\n    const oldGt = oldHtml.indexOf(\">\");\n    const newGt = newHtml.indexOf(\">\");\n    if (oldGt === -1 || newGt === -1) return false;\n    if (oldHtml.length - oldGt !== newHtml.length - newGt) return false;\n    if (oldHtml.slice(oldGt) !== newHtml.slice(newGt)) return false;\n    if (containsDataMorphSkip(oldHtml) || containsDataMorphSkip(newHtml)) return false;\n    const oldTag = parseOpeningTag(oldHtml, oldGt);\n    const newTag = parseOpeningTag(newHtml, newGt);\n    if (oldTag === null || newTag === null) return false;\n    if (oldTag.tagName !== newTag.tagName) return false;\n    for (const name of oldTag.attrs.keys()) {\n      if (name.indexOf(\":\") !== -1) return false;\n    }\n    for (const name of newTag.attrs.keys()) {\n      if (name.indexOf(\":\") !== -1) return false;\n    }\n    const liveTagUpper = liveNode.tagName;\n    for (const [name, rawValue] of newTag.attrs) {\n      const oldValue = oldTag.attrs.get(name);\n      if (oldValue === rawValue) continue;\n      const value = unescapeAttrValue(rawValue);\n      liveNode.setAttribute(name, value);\n      syncFormProp(liveNode, name, value, true);\n    }\n    for (const name of oldTag.attrs.keys()) {\n      if (newTag.attrs.has(name)) continue;\n      if (isUserAgentOwnedAttr2(liveTagUpper, name)) continue;\n      liveNode.removeAttribute(name);\n      syncFormProp(liveNode, name, \"\", false);\n    }\n    return true;\n  }\n  function tryTextContentFastPath(liveNode, oldHtml, newHtml) {\n    if (containsDataMorphSkip(oldHtml) || containsDataMorphSkip(newHtml)) return false;\n    let p2 = 0;\n    const minLen = Math.min(oldHtml.length, newHtml.length);\n    while (p2 < minLen && oldHtml.charCodeAt(p2) === newHtml.charCodeAt(p2)) p2++;\n    let s2 = 0;\n    const maxS = minLen - p2;\n    while (s2 < maxS && oldHtml.charCodeAt(oldHtml.length - 1 - s2) === newHtml.charCodeAt(newHtml.length - 1 - s2)) {\n      s2++;\n    }\n    const oldWinEnd = oldHtml.length - s2;\n    const newWinEnd = newHtml.length - s2;\n    if (!isPureTextWindow(oldHtml, p2, oldWinEnd)) return false;\n    if (!isPureTextWindow(newHtml, p2, newWinEnd)) return false;\n    if (p2 === 0) return false;\n    const boundaryCc = oldHtml.charCodeAt(p2 - 1);\n    if (boundaryCc === LT || boundaryCc === DQUOTE || boundaryCc === SQUOTE || boundaryCc === EQ || boundaryCc === AMP) return false;\n    const textStart = lastIndexOfChar(oldHtml, GT, p2 - 1);\n    if (textStart === -1) return false;\n    const textEnd = oldHtml.indexOf(\"<\", p2);\n    if (textEnd === -1) return false;\n    if (textEnd < oldWinEnd) return false;\n    const newTextEnd = textEnd + (newHtml.length - oldHtml.length);\n    const oldText = oldHtml.slice(textStart + 1, textEnd);\n    const newText = newHtml.slice(textStart + 1, newTextEnd);\n    if (oldHtml.lastIndexOf(\"<!--kfb\", textStart) !== -1) return false;\n    const textIdx = countTextNodesBefore(oldHtml, textStart + 1);\n    const targetNode = nthTextNodeDescendant(liveNode, textIdx);\n    if (targetNode === null) return false;\n    if (targetNode.nodeValue !== oldText) return false;\n    targetNode.nodeValue = newText;\n    const host = targetNode.parentNode;\n    if (host !== null && host.tagName === \"TEXTAREA\" && host !== document.activeElement) {\n      host.value = newText;\n    }\n    return true;\n  }\n  function containsDataMorphSkip(html) {\n    return html.indexOf(\"data-morph-skip\") !== -1;\n  }\n  function isPureTextWindow(html, start, end) {\n    for (let i2 = start; i2 < end; i2++) {\n      const cc = html.charCodeAt(i2);\n      if (cc === LT || cc === GT || cc === DQUOTE || cc === SQUOTE || cc === AMP || cc === EQ) return false;\n    }\n    return true;\n  }\n  function lastIndexOfChar(html, target, beforeInclusive) {\n    for (let i2 = beforeInclusive; i2 >= 0; i2--) {\n      if (html.charCodeAt(i2) === target) return i2;\n    }\n    return -1;\n  }\n  function countTextNodesBefore(html, beforePos) {\n    let count = 0;\n    let i2 = 0;\n    while (i2 < beforePos) {\n      if (html.charCodeAt(i2) === LT) {\n        while (i2 < beforePos && html.charCodeAt(i2) !== GT) i2++;\n        i2++;\n      } else {\n        const start = i2;\n        while (i2 < beforePos && html.charCodeAt(i2) !== LT) i2++;\n        if (i2 > start) count++;\n      }\n    }\n    return count;\n  }\n  function nthTextNodeDescendant(root, n2) {\n    let count = 0;\n    let result = null;\n    function walk(node) {\n      for (let c2 = node.firstChild; c2 !== null; c2 = c2.nextSibling) {\n        if (result !== null) return;\n        if (c2.nodeType === TEXT_NODE2) {\n          if (count === n2) {\n            result = c2;\n            return;\n          }\n          count++;\n        } else if (c2.nodeType === ELEMENT_NODE2) {\n          walk(c2);\n        }\n      }\n    }\n    walk(root);\n    return result;\n  }\n  function parseOpeningTag(html, gtPos) {\n    if (html.charCodeAt(0) !== LT) return null;\n    let i2 = 1;\n    let end = gtPos;\n    if (i2 < end && html.charCodeAt(end - 1) === SLASH) end -= 1;\n    const nameStart = i2;\n    while (i2 < end) {\n      const cc = html.charCodeAt(i2);\n      if (isWhitespace(cc)) break;\n      i2++;\n    }\n    const tagName = html.slice(nameStart, i2);\n    if (tagName.length === 0) return null;\n    const attrs = /* @__PURE__ */ new Map();\n    while (i2 < end) {\n      while (i2 < end && isWhitespace(html.charCodeAt(i2))) i2++;\n      if (i2 >= end) break;\n      const aNameStart = i2;\n      while (i2 < end) {\n        const cc = html.charCodeAt(i2);\n        if (cc === EQ || isWhitespace(cc)) break;\n        i2++;\n      }\n      const aName = html.slice(aNameStart, i2);\n      if (aName.length === 0) return null;\n      while (i2 < end && isWhitespace(html.charCodeAt(i2))) i2++;\n      if (i2 < end && html.charCodeAt(i2) === EQ) {\n        i2++;\n        while (i2 < end && isWhitespace(html.charCodeAt(i2))) i2++;\n        if (i2 >= end) return null;\n        const q = html.charCodeAt(i2);\n        if (q !== DQUOTE && q !== SQUOTE) return null;\n        i2++;\n        const vStart = i2;\n        while (i2 < end && html.charCodeAt(i2) !== q) i2++;\n        if (i2 >= end) return null;\n        attrs.set(aName, html.slice(vStart, i2));\n        i2++;\n      } else {\n        attrs.set(aName, \"\");\n      }\n    }\n    return { tagName, attrs };\n  }\n  function unescapeAttrValue(s2) {\n    if (s2.indexOf(\"&\") === -1) return s2;\n    return s2.replace(/&quot;/g, '\"').replace(/&#39;/g, \"'\").replace(/&lt;/g, \"<\").replace(/&gt;/g, \">\").replace(/&amp;/g, \"&\");\n  }\n  function isUserAgentOwnedAttr2(tagNameUpper, name) {\n    return name === \"open\" && (tagNameUpper === \"DETAILS\" || tagNameUpper === \"DIALOG\");\n  }\n  function reconcileGranular(binding, patches) {\n    const { liveParent } = binding;\n    const items = binding.items;\n    const focusSnap = captureFocus(liveParent);\n    let i2 = 0;\n    while (i2 < patches.length) {\n      const patch = patches[i2];\n      if (patch.type === \"replace\") {\n        i2 += 1;\n        continue;\n      }\n      if (patch.type === \"update\") {\n        let runEnd = i2 + 1;\n        while (runEnd < patches.length && patches[runEnd].type === \"update\") {\n          runEnd += 1;\n        }\n        const runLen = runEnd - i2;\n        if (runLen === 1) {\n          applySingleUpdate(liveParent, items, patch);\n        } else {\n          applyBulkUpdate(liveParent, items, patches, i2, runEnd);\n        }\n        i2 = runEnd;\n        continue;\n      }\n      if (patch.type === \"insert\") {\n        let runEnd = i2 + 1;\n        while (runEnd < patches.length && patches[runEnd].type === \"insert\" && patches[runEnd].index === patches[runEnd - 1].index + 1) {\n          runEnd += 1;\n        }\n        const runLen = runEnd - i2;\n        if (runLen === 1) {\n          applySingleInsert(liveParent, items, patch, endAnchor(binding));\n        } else {\n          applyBulkInsert(liveParent, items, patches, i2, runEnd, endAnchor(binding));\n        }\n        i2 = runEnd;\n        continue;\n      }\n      if (patch.type === \"remove\") {\n        const entry = items[patch.index];\n        disposeRowBindings(entry.bindingDisposers);\n        liveParent.removeChild(entry.node);\n        items.splice(patch.index, 1);\n        i2 += 1;\n        continue;\n      }\n      if (patch.type === \"move\") {\n        const moved = items[patch.from];\n        let anchorIdx = patch.to;\n        if (patch.from < patch.to) anchorIdx += 1;\n        const anchor = anchorIdx < items.length ? items[anchorIdx].node : endAnchor(binding);\n        moveNode(liveParent, moved.node, anchor);\n        items.splice(patch.from, 1);\n        items.splice(patch.to, 0, moved);\n        i2 += 1;\n        continue;\n      }\n    }\n    if (focusSnap !== null) restoreFocus(focusSnap);\n    if (items.length > 0) {\n      devHooks.missingRowKey?.(items[0].node, items[0].html, binding);\n    }\n  }\n  function applySingleInsert(liveParent, items, patch, tailAnchor) {\n    const { html } = patch;\n    const newNode = parseSingleRow(html, patch.index, liveParent);\n    const anchor = patch.index < items.length ? items[patch.index].node : tailAnchor;\n    liveParent.insertBefore(newNode, anchor);\n    items.splice(patch.index, 0, {\n      ref: patch.item,\n      cacheKey: void 0,\n      html,\n      node: newNode,\n      bindings: patch.bindings,\n      // KF-294: wire the inserted row's fine-grained bindings to its new node.\n      bindingDisposers: wireRowIfBound(newNode, patch.bindings)\n    });\n  }\n  function wireRowIfBound(node, bindings) {\n    return bindings !== void 0 && bindings.length > 0 ? wireRowBindings(node, bindings) : void 0;\n  }\n  function applySingleUpdate(liveParent, items, patch) {\n    const { html } = patch;\n    const oldEntry = items[patch.index];\n    if (html === oldEntry.html) {\n      items[patch.index] = reuseBound(patch, html, oldEntry);\n      return;\n    }\n    if (tryAttributeOnlyFastPath(oldEntry.node, oldEntry.html, html) || tryTextContentFastPath(oldEntry.node, oldEntry.html, html)) {\n      items[patch.index] = reuseBound(patch, html, oldEntry);\n      return;\n    }\n    const newNode = parseSingleRow(html, patch.index, liveParent);\n    applyParsedRowUpdate(liveParent, items, patch, html, newNode);\n  }\n  function applyParsedRowUpdate(liveParent, items, patch, html, newNode) {\n    const oldEntry = items[patch.index];\n    if (oldEntry.node.tagName === newNode.tagName) {\n      _morphElement(oldEntry.node, newNode);\n      items[patch.index] = reuseBound(patch, html, oldEntry);\n    } else {\n      disposeRowBindings(oldEntry.bindingDisposers);\n      liveParent.replaceChild(newNode, oldEntry.node);\n      items[patch.index] = {\n        ref: patch.item,\n        cacheKey: void 0,\n        html,\n        node: newNode,\n        bindings: patch.bindings,\n        bindingDisposers: wireRowIfBound(newNode, patch.bindings)\n      };\n    }\n  }\n  function reuseBound(patch, html, oldEntry) {\n    const kept = carryOrRewireRowBindings(\n      oldEntry.node,\n      oldEntry.bindings,\n      oldEntry.bindingDisposers,\n      patch.bindings\n    );\n    return {\n      ref: patch.item,\n      cacheKey: void 0,\n      html,\n      node: oldEntry.node,\n      bindings: kept.bindings,\n      bindingDisposers: kept.bindingDisposers\n    };\n  }\n  function applyBulkUpdate(liveParent, items, patches, start, end) {\n    const morphChanges = [];\n    for (let k = start; k < end; k++) {\n      const p2 = patches[k];\n      const oldEntry = items[p2.index];\n      if (p2.html === oldEntry.html) {\n        items[p2.index] = reuseBound(p2, p2.html, oldEntry);\n        continue;\n      }\n      if (tryAttributeOnlyFastPath(oldEntry.node, oldEntry.html, p2.html) || tryTextContentFastPath(oldEntry.node, oldEntry.html, p2.html)) {\n        items[p2.index] = reuseBound(p2, p2.html, oldEntry);\n        continue;\n      }\n      morphChanges.push({ patchIdx: k, html: p2.html });\n    }\n    if (morphChanges.length === 0) return;\n    const { content, count } = parseRowTemplate(morphChanges.map((c2) => c2.html).join(\"\"), liveParent);\n    if (count !== morphChanges.length) {\n      throw findOffendingChange(patches, morphChanges, liveParent);\n    }\n    const newNodes = collectTemplateChildren(content, morphChanges.length);\n    for (let k = 0; k < morphChanges.length; k++) {\n      const c2 = morphChanges[k];\n      const p2 = patches[c2.patchIdx];\n      applyParsedRowUpdate(liveParent, items, p2, c2.html, newNodes[k]);\n    }\n  }\n  function applyBulkInsert(liveParent, items, patches, start, end, tailAnchor) {\n    const startIdx = patches[start].index;\n    const htmls = new Array(end - start);\n    for (let k = start; k < end; k++) {\n      htmls[k - start] = patches[k].html;\n    }\n    const { content, count } = parseRowTemplate(htmls.join(\"\"), liveParent);\n    if (count !== htmls.length) {\n      throw findOffendingInsert(patches, start, htmls, liveParent);\n    }\n    const newNodes = collectTemplateChildren(content, end - start);\n    const anchor = startIdx < items.length ? items[startIdx].node : tailAnchor;\n    liveParent.insertBefore(content, anchor);\n    const newEntries = new Array(end - start);\n    for (let k = 0; k < newEntries.length; k++) {\n      const p2 = patches[start + k];\n      newEntries[k] = {\n        ref: p2.item,\n        cacheKey: void 0,\n        html: htmls[k],\n        node: newNodes[k],\n        bindings: p2.bindings,\n        bindingDisposers: wireRowIfBound(newNodes[k], p2.bindings)\n        // KF-294\n      };\n    }\n    items.splice(startIdx, 0, ...newEntries);\n  }\n  function findOffendingInsert(patches, start, htmls, liveParent) {\n    for (let i2 = 0; i2 < htmls.length; i2++) {\n      if (parseRowTemplate(htmls[i2], liveParent).count !== 1) {\n        return rowContractError(patches[start + i2].index, htmls[i2], liveParent);\n      }\n    }\n    return new Error(\"each(): bulk-insert mismatch with no per-row offender (kerf bug).\");\n  }\n  function findOffendingChange(patches, changes, liveParent) {\n    for (const c2 of changes) {\n      if (parseRowTemplate(c2.html, liveParent).count !== 1) {\n        return rowContractError(patches[c2.patchIdx].index, c2.html, liveParent);\n      }\n    }\n    return new Error(\"each(): bulk-update mismatch with no per-row offender (kerf bug).\");\n  }\n  function tryInPlaceContentUpdate(binding, listSeg) {\n    const oldItems = binding.items;\n    const items = listSeg.items;\n    const n2 = items.length;\n    if (n2 === 0 || n2 !== oldItems.length) return false;\n    for (let i2 = 0; i2 < n2; i2++) {\n      if (items[i2].ref !== oldItems[i2].ref) return false;\n    }\n    const { liveParent } = binding;\n    const newRecord = new Array(n2);\n    const focusSnap = captureFocus(liveParent);\n    for (let i2 = 0; i2 < n2; i2++) {\n      newRecord[i2] = updateRowInPlace(liveParent, oldItems[i2], items[i2], i2);\n    }\n    if (focusSnap !== null) restoreFocus(focusSnap);\n    binding.items = newRecord;\n    devHooks.missingRowKey?.(newRecord[0].node, newRecord[0].html, binding);\n    return true;\n  }\n  function updateRowInPlace(liveParent, old, ni, index) {\n    if (old.html === ni.html || tryAttributeOnlyFastPath(old.node, old.html, ni.html) || tryTextContentFastPath(old.node, old.html, ni.html)) {\n      const kept = carryOrRewireRowBindings(old.node, old.bindings, old.bindingDisposers, ni.bindings);\n      return {\n        ref: ni.ref,\n        cacheKey: ni.cacheKey,\n        html: ni.html,\n        node: old.node,\n        bindings: kept.bindings,\n        bindingDisposers: kept.bindingDisposers\n      };\n    }\n    const newNode = parseSingleRow(ni.html, index, liveParent);\n    if (old.node.tagName === newNode.tagName) {\n      _morphElement(old.node, newNode);\n      const kept = carryOrRewireRowBindings(old.node, old.bindings, old.bindingDisposers, ni.bindings);\n      return {\n        ref: ni.ref,\n        cacheKey: ni.cacheKey,\n        html: ni.html,\n        node: old.node,\n        bindings: kept.bindings,\n        bindingDisposers: kept.bindingDisposers\n      };\n    }\n    disposeRowBindings(old.bindingDisposers);\n    liveParent.replaceChild(newNode, old.node);\n    const fresh = carryOrRewireRowBindings(newNode, void 0, void 0, ni.bindings);\n    return {\n      ref: ni.ref,\n      cacheKey: ni.cacheKey,\n      html: ni.html,\n      node: newNode,\n      bindings: fresh.bindings,\n      bindingDisposers: fresh.bindingDisposers\n    };\n  }\n  function reconcileSnapshot(binding, listSeg) {\n    if (tryInPlaceContentUpdate(binding, listSeg)) return;\n    const { liveParent } = binding;\n    const { newRecord, prevIdx, removedItems, freshIndices, freshHtmls } = classifyItems(binding.items, listSeg);\n    const tailAnchor = endAnchor(binding);\n    buildFreshNodes(newRecord, freshIndices, freshHtmls, liveParent);\n    const focusSnap = captureFocus(liveParent);\n    removeOldNodes(liveParent, removedItems);\n    applyMoves(liveParent, newRecord, prevIdx, lis(prevIdx), tailAnchor);\n    if (focusSnap !== null) restoreFocus(focusSnap);\n    binding.items = newRecord;\n    if (newRecord.length > 0) {\n      devHooks.missingRowKey?.(newRecord[0].node, newRecord[0].html, binding);\n    }\n  }\n  function classifyItems(oldItems, listSeg) {\n    const oldByRef = /* @__PURE__ */ new Map();\n    for (let i2 = 0; i2 < oldItems.length; i2++) {\n      oldByRef.set(oldItems[i2].ref, [oldItems[i2], i2]);\n    }\n    const newRecord = new Array(listSeg.items.length);\n    const prevIdx = new Array(listSeg.items.length);\n    const removedItems = [];\n    const freshIndices = [];\n    const freshHtmls = [];\n    for (let i2 = 0; i2 < listSeg.items.length; i2++) {\n      const ni = listSeg.items[i2];\n      const oi = oldByRef.get(ni.ref);\n      if (oi !== void 0) {\n        oldByRef.delete(ni.ref);\n        if (oi[0].html === ni.html) {\n          newRecord[i2] = oi[0];\n          prevIdx[i2] = oi[1];\n          continue;\n        }\n        removedItems.push(oi[0]);\n      }\n      newRecord[i2] = {\n        // `node` placeholder is filled by `buildFreshNodes`; its parse-count\n        // check guarantees every fresh index gets a real element before use.\n        ref: ni.ref,\n        cacheKey: ni.cacheKey,\n        html: ni.html,\n        node: null,\n        bindings: ni.bindings\n      };\n      prevIdx[i2] = -1;\n      freshIndices.push(i2);\n      freshHtmls.push(ni.html);\n    }\n    for (const [, orphan] of oldByRef) removedItems.push(orphan[0]);\n    return { newRecord, prevIdx, removedItems, freshIndices, freshHtmls };\n  }\n  function buildFreshNodes(newRecord, freshIndices, freshHtmls, liveParent) {\n    if (freshHtmls.length === 0) return;\n    const { content, count } = parseRowTemplate(freshHtmls.join(\"\"), liveParent);\n    if (count !== freshHtmls.length) {\n      throw findOffendingRow(newRecord, freshIndices, freshHtmls, liveParent);\n    }\n    let node = content.firstElementChild;\n    for (const idx of freshIndices) {\n      const next = node.nextElementSibling;\n      const item = newRecord[idx];\n      item.node = node;\n      if (item.bindings !== void 0 && item.bindings.length > 0) {\n        item.bindingDisposers = wireRowBindings(item.node, item.bindings);\n      }\n      node = next;\n    }\n  }\n  function findOffendingRow(newRecord, freshIndices, freshHtmls, liveParent) {\n    for (let i2 = 0; i2 < freshHtmls.length; i2++) {\n      if (parseRowTemplate(freshHtmls[i2], liveParent).count !== 1) {\n        return rowContractError(freshIndices[i2], newRecord[freshIndices[i2]].html, liveParent);\n      }\n    }\n    return new Error(\"each(): bulk-parse mismatch with no per-row offender (kerf bug).\");\n  }\n  function removeOldNodes(liveParent, removedItems) {\n    for (const item of removedItems) {\n      disposeRowBindings(item.bindingDisposers);\n      if (item.node.parentElement === liveParent) liveParent.removeChild(item.node);\n    }\n  }\n  function applyMoves(liveParent, newRecord, prevIdx, stable, tailAnchor) {\n    let nextSibling = tailAnchor;\n    for (let i2 = newRecord.length - 1; i2 >= 0; i2--) {\n      const node = newRecord[i2].node;\n      if (prevIdx[i2] === -1 || !stable.has(i2)) {\n        moveNode(liveParent, node, nextSibling);\n      }\n      nextSibling = node;\n    }\n  }\n  function lis(arr) {\n    const tails = [];\n    const tailIdx = [];\n    const prev = new Array(arr.length);\n    for (let i2 = 0; i2 < arr.length; i2++) {\n      const v2 = arr[i2];\n      if (v2 === -1) {\n        prev[i2] = -1;\n        continue;\n      }\n      let lo = 0;\n      let hi = tails.length;\n      while (lo < hi) {\n        const mid = lo + hi >> 1;\n        if (tails[mid] < v2) lo = mid + 1;\n        else hi = mid;\n      }\n      prev[i2] = lo > 0 ? tailIdx[lo - 1] : -1;\n      tails[lo] = v2;\n      tailIdx[lo] = i2;\n    }\n    const out = /* @__PURE__ */ new Set();\n    let k = tailIdx.length > 0 ? tailIdx[tailIdx.length - 1] : -1;\n    while (k !== -1) {\n      out.add(k);\n      k = prev[k];\n    }\n    return out;\n  }\n  function reconcileList(binding, listSeg) {\n    if (listSeg.patches !== void 0 && binding.items.length > 0) {\n      reconcileGranular(binding, listSeg.patches);\n      return;\n    }\n    reconcileSnapshot(binding, listSeg);\n  }\n  var MOUNTED_MARKER = /* @__PURE__ */ Symbol.for(\"kerfjs.mounted\");\n  var NESTED_MOUNT_MSG = \"mount: rootEl is already inside (or contains) a mounted tree. kerf supports one mount per tree \\u2014 compose with plain functions that return JSX instead of nesting mounts.\";\n  function isMounted(el) {\n    return el[MOUNTED_MARKER] === true;\n  }\n  function setMounted(el, on) {\n    if (on) {\n      el[MOUNTED_MARKER] = true;\n    } else {\n      delete el[MOUNTED_MARKER];\n    }\n  }\n  function describeEl(el) {\n    const tag = el.tagName.toLowerCase();\n    const id = el.id ? `#${el.id}` : \"\";\n    return `<${tag}${id}>`;\n  }\n  function assertNotInsideMountedTree(rootEl) {\n    if (isMounted(rootEl)) {\n      throw new Error(\n        `mount: ${describeEl(rootEl)} is already mounted. Call the disposer returned by the first mount() before mounting again. kerf supports one mount per element \\u2014 compose with plain functions that return JSX instead of nesting mounts.`\n      );\n    }\n    let ancestor = rootEl.parentElement;\n    while (ancestor !== null) {\n      if (isMounted(ancestor)) throw new Error(NESTED_MOUNT_MSG);\n      ancestor = ancestor.parentElement;\n    }\n    const stack = [];\n    for (let i2 = 0; i2 < rootEl.children.length; i2++) stack.push(rootEl.children[i2]);\n    while (stack.length > 0) {\n      const cur = stack.pop();\n      if (isMounted(cur)) throw new Error(NESTED_MOUNT_MSG);\n      for (let i2 = 0; i2 < cur.children.length; i2++) stack.push(cur.children[i2]);\n    }\n  }\n  function mount(rootEl, render2) {\n    if (rootEl == null) {\n      throw new Error(\n        'mount: rootEl is null/undefined \\u2014 pass the live element, e.g. mount(document.getElementById(\"app\")!, render). A common cause is a typo in the id or selector that returns null at runtime even though the TypeScript types say HTMLElement.'\n      );\n    }\n    const owner = rootEl.ownerDocument;\n    if (owner !== document) {\n      if (owner.defaultView === null) document.adoptNode(rootEl);\n    }\n    assertNotInsideMountedTree(rootEl);\n    setMounted(rootEl, true);\n    const listenerWarnObserver = devHooks.listenerRebuild?.(rootEl) ?? null;\n    const bindings = /* @__PURE__ */ new Map();\n    const renderCtx = {\n      counter: 0,\n      caches: /* @__PURE__ */ new Map(),\n      bindingCounts: /* @__PURE__ */ new Map(),\n      bindingSources: /* @__PURE__ */ new Map(),\n      keysThisRender: /* @__PURE__ */ new Set(),\n      shiftCandidates: [],\n      warnedShiftIds: /* @__PURE__ */ new Set(),\n      rebuiltLists: /* @__PURE__ */ new Set()\n    };\n    const bindingCtx = newBindingContext();\n    let bindingDisposers = [];\n    let prevWiredBindings = [];\n    let isFirst = true;\n    let prevStaticHtml = \"\";\n    const valueOnlyWarnCtx = { warned: false };\n    const runRenderPass = () => {\n      renderCtx.counter = 0;\n      renderCtx.keysThisRender.clear();\n      renderCtx.shiftCandidates.length = 0;\n      bindingCtx.counter = 0;\n      bindingCtx.list = [];\n      _setRenderContext(renderCtx);\n      _setBindingContext(bindingCtx);\n      try {\n        return render2();\n      } finally {\n        _setRenderContext(null);\n        _setBindingContext(null);\n      }\n    };\n    const disposeEffect = effect(() => {\n      let result = runRenderPass();\n      const countChanged = renderCtx.previousCallCount !== void 0 && renderCtx.previousCallCount !== renderCtx.counter;\n      if (countChanged) {\n        for (const id of renderCtx.shiftCandidates) {\n          if (renderCtx.warnedShiftIds.has(id)) continue;\n          renderCtx.warnedShiftIds.add(id);\n          devHooks.listIdShift?.(id);\n        }\n        _resetCallOrderListState(renderCtx);\n        result = runRenderPass();\n      }\n      let segment = resultToSegment(result);\n      if (isFirst) {\n        runFirstRender(rootEl, segment, bindings);\n        prevStaticHtml = flattenWithoutListItems(segment);\n        devHooks.parserRepair?.(prevStaticHtml);\n        bindingDisposers = wireBindings(rootEl, bindingCtx, bindingDisposers);\n        if (devHooks.staleBindingEnabled?.() === true) prevWiredBindings = bindingCtx.list;\n        isFirst = false;\n      } else {\n        let nextStaticHtml = runSubsequentRender(\n          rootEl,\n          segment,\n          bindings,\n          renderCtx,\n          prevStaticHtml,\n          valueOnlyWarnCtx\n        );\n        if (anyRebuiltListIsGranular(segment, renderCtx.rebuiltLists)) {\n          for (const id of renderCtx.rebuiltLists) renderCtx.bindingCounts.delete(id);\n          result = runRenderPass();\n          segment = resultToSegment(result);\n          nextStaticHtml = runSubsequentRender(\n            rootEl,\n            segment,\n            bindings,\n            renderCtx,\n            prevStaticHtml,\n            valueOnlyWarnCtx\n          );\n        }\n        if (nextStaticHtml !== prevStaticHtml) {\n          bindingDisposers = wireBindings(rootEl, bindingCtx, bindingDisposers);\n          if (devHooks.staleBindingEnabled?.() === true) prevWiredBindings = bindingCtx.list;\n        } else {\n          devHooks.staleBinding?.(prevWiredBindings, bindingCtx.list);\n        }\n        prevStaticHtml = nextStaticHtml;\n      }\n      const expectedCounts = devHooks.listInvariantsEnabled?.() === true ? /* @__PURE__ */ new Map() : null;\n      for (const listSeg of collectLists(segment).values()) {\n        const binding = bindings.get(listSeg.id);\n        if (binding === void 0) {\n          throw new Error(\n            \"mount: an each() list appeared in the render output but its marker never reached the live DOM. The most common cause is an each() introduced inside a data-morph-skip subtree on a re-render \\u2014 the morph leaves that subtree untouched, so the list can never bind. Move the each() outside the skipped subtree, or remove data-morph-skip from its ancestor.\"\n          );\n        }\n        reconcileList(binding, listSeg);\n        renderCtx.bindingCounts.set(listSeg.id, binding.items.length);\n        renderCtx.bindingSources.set(listSeg.id, listSeg.source);\n        expectedCounts?.set(\n          listSeg.id,\n          listSeg.patches !== void 0 && listSeg.source !== void 0 ? listSeg.source.value.length : listSeg.items.length\n        );\n      }\n      renderCtx.previousCallCount = renderCtx.counter;\n      devHooks.listInvariants?.(rootEl, bindings, expectedCounts ?? void 0);\n    });\n    return () => {\n      disposeEffect();\n      for (const d2 of bindingDisposers) d2();\n      bindingDisposers = [];\n      for (const b2 of bindings.values()) {\n        for (const item of b2.items) disposeRowBindings(item.bindingDisposers);\n      }\n      listenerWarnObserver?.disconnect();\n      setMounted(rootEl, false);\n    };\n  }\n  function runFirstRender(rootEl, segment, bindings) {\n    rootEl.innerHTML = flatten(segment, true);\n    bindListsFromMarkers(rootEl, segment, bindings, true);\n  }\n  function runSubsequentRender(rootEl, segment, bindings, renderCtx, prevStaticHtml, valueOnlyWarnCtx) {\n    renderCtx.rebuiltLists.clear();\n    const currentStaticHtml = flattenWithoutListItems(segment);\n    if (currentStaticHtml === prevStaticHtml) {\n      return prevStaticHtml;\n    }\n    devHooks.valueOnlyRerender?.(prevStaticHtml, currentStaticHtml, valueOnlyWarnCtx);\n    cleanupOrphanBindings(segment, bindings, renderCtx);\n    const template = rootEl.cloneNode(false);\n    template.innerHTML = currentStaticHtml;\n    morph(rootEl, template, collectOwnedItems(bindings));\n    bindListsFromMarkers(rootEl, segment, bindings, false, renderCtx.rebuiltLists);\n    return currentStaticHtml;\n  }\n  function coerceRenderResult(result) {\n    if (result === null || result === void 0) return \"\";\n    if (result === false || result === true) return \"\";\n    return String(result);\n  }\n  function resultToSegment(result) {\n    return isSafeHtml(result) ? result.__segment ?? { kind: \"static\", html: result.__html } : { kind: \"static\", html: coerceRenderResult(result) };\n  }\n  function anyRebuiltListIsGranular(segment, rebuilt) {\n    if (rebuilt.size === 0) return false;\n    const lists = collectLists(segment);\n    for (const id of rebuilt) {\n      if (lists.get(id)?.patches !== void 0) return true;\n    }\n    return false;\n  }\n  function bindListsFromMarkers(rootEl, segment, bindings, inlinedItems, rebuiltLists) {\n    const lists = collectLists(segment);\n    const found = [];\n    collectComments2(rootEl, found);\n    for (const marker of found) {\n      if (!marker.data.startsWith(LIST_MARKER_PREFIX)) continue;\n      const id = marker.data.slice(LIST_MARKER_PREFIX.length);\n      const existing = bindings.get(id);\n      if (existing !== void 0) {\n        if (existing.marker === marker && rootEl.contains(existing.marker)) continue;\n        for (const item of existing.items) {\n          disposeRowBindings(item.bindingDisposers);\n          if (rootEl.contains(item.node)) {\n            item.node.parentElement?.removeChild(item.node);\n          }\n        }\n        bindings.delete(id);\n        rebuiltLists?.add(id);\n        devHooks.listRebind?.(id, marker.parentElement);\n      }\n      const listSeg = lists.get(id);\n      const liveParent = marker.parentElement;\n      const items = [];\n      if (inlinedItems) {\n        let next = marker.nextElementSibling;\n        for (let i2 = 0; i2 < listSeg.items.length && next !== null; i2++) {\n          validateInlinedRowMatch(listSeg.items[i2].html, i2, next, liveParent);\n          const rowBindings = listSeg.items[i2].bindings;\n          const bound = {\n            ref: listSeg.items[i2].ref,\n            cacheKey: listSeg.items[i2].cacheKey,\n            html: listSeg.items[i2].html,\n            node: next,\n            bindings: rowBindings\n          };\n          if (rowBindings !== void 0 && rowBindings.length > 0) {\n            bound.bindingDisposers = wireRowBindings(next, rowBindings);\n          }\n          items.push(bound);\n          next = next.nextElementSibling;\n        }\n      }\n      const binding = { liveParent, items, marker };\n      if (items.length > 0) {\n        devHooks.missingRowKey?.(items[0].node, items[0].html, binding);\n      }\n      devHooks.eachInMorphSkip?.(id, liveParent, rootEl);\n      bindings.set(id, binding);\n    }\n  }\n  function validateInlinedRowMatch(expectedHtml, index, boundEl, liveParent) {\n    if (boundEl.outerHTML === expectedHtml) return;\n    const { content, count } = parseRowTemplate(expectedHtml, liveParent);\n    if (count !== 1) throw rowContractError(index, expectedHtml, liveParent);\n    const expectedTag = content.firstElementChild.tagName;\n    if (boundEl.tagName !== expectedTag) throw rowStructureError(index, boundEl.tagName, expectedTag);\n  }\n  function rowStructureError(index, gotTag, wantTag) {\n    const got = gotTag.toLowerCase();\n    const want = wantTag.toLowerCase();\n    return new Error(\n      `each(): row ${index} renders <${want}>, but the HTML parser wrapped the rows in <${got}> \\u2014 so kerf cannot bind one row per element. This happens when an each() of <${want}> sits directly inside a table: the parser inserts <${got}> around the whole run. Put the each() inside an explicit <${got}> (e.g. <table><${got}>{each(...)}</${got}></table>) so the rows are the direct children kerf binds.`\n    );\n  }\n  function collectOwnedItems(bindings) {\n    const owned = /* @__PURE__ */ new Set();\n    for (const b2 of bindings.values()) {\n      for (const item of b2.items) owned.add(item.node);\n    }\n    return owned;\n  }\n  function cleanupOrphanBindings(segment, bindings, renderCtx) {\n    const liveIds = collectLists(segment);\n    for (const [id, binding] of bindings) {\n      if (liveIds.has(id)) continue;\n      for (const item of binding.items) {\n        disposeRowBindings(item.bindingDisposers);\n        if (item.node.parentElement !== null) {\n          item.node.parentElement.removeChild(item.node);\n        }\n      }\n      if (binding.marker.parentElement !== null) {\n        binding.marker.parentElement.removeChild(binding.marker);\n      }\n      bindings.delete(id);\n      renderCtx.bindingCounts.delete(id);\n      renderCtx.bindingSources.delete(id);\n      renderCtx.caches.delete(id);\n    }\n  }\n  function collectComments2(node, out) {\n    for (let c2 = node.firstChild; c2 !== null; c2 = c2.nextSibling) {\n      if (c2.nodeType === Node.COMMENT_NODE) out.push(c2);\n      else if (c2.nodeType === Node.ELEMENT_NODE) collectComments2(c2, out);\n    }\n  }\n\n  // node_modules/kerfjs/dist/chunk-KEZTD6H4.js\n  var NON_BUBBLING = /* @__PURE__ */ new Set([\n    \"focus\",\n    \"blur\",\n    \"scroll\",\n    \"load\",\n    \"error\",\n    \"mouseenter\",\n    \"mouseleave\"\n  ]);\n  function assertValidSelector(selector, fn) {\n    try {\n      document.createElement(\"div\").matches(selector);\n    } catch {\n      throw new Error(\n        `${fn}: invalid selector \"${selector}\". Pass a valid CSS selector (e.g. '[data-action=\"add\"]', '.btn', 'input').`\n      );\n    }\n  }\n  function makeListener(rootEl, selector, handler, match) {\n    return (event) => {\n      const target = event.target;\n      if (!(target instanceof Element)) return;\n      const matched = match === \"direct\" ? target.matches(selector) ? target : null : target.closest(selector);\n      if (matched !== null && rootEl.contains(matched)) {\n        handler(event, matched);\n      }\n    };\n  }\n  function delegate(rootEl, type, selector, handler, options) {\n    assertValidSelector(selector, \"delegate\");\n    devHooks.delegateInEffect?.(\"delegate\");\n    const listener = makeListener(rootEl, selector, handler, options?.match ?? \"closest\");\n    const capture = NON_BUBBLING.has(type);\n    rootEl.addEventListener(type, listener, capture);\n    return () => {\n      rootEl.removeEventListener(type, listener, capture);\n    };\n  }\n\n  // src/studio/client.tsx\n  var bootstrap = window.__DOMOTION_STUDIO__ ?? {\n    workspaceRoot: \"\",\n    path: \"demo.studio.json\",\n    project: null,\n    issues: [],\n    error: \"\"\n  };\n  var projectPath = signal(bootstrap.path);\n  var project = signal(bootstrap.project);\n  var newTitle = signal(bootstrap.project?.narrative.title ?? \"Product demo\");\n  var issues = signal(bootstrap.issues);\n  var message = signal(bootstrap.error);\n  var messageKind = signal(bootstrap.error === \"\" ? \"info\" : \"error\");\n  var busy = signal(false);\n  var dirty = signal(false);\n  var generation = signal(\n    bootstrap.project == null ? null : {\n      artifactCount: bootstrap.project.artifacts.length,\n      scenes: bootstrap.project.scenes.map((scene) => ({\n        id: scene.id,\n        generated: bootstrap.project.artifacts.some((artifact) => artifact.sceneIds?.includes(scene.id) === true)\n      }))\n    }\n  );\n  var openAnnotations = g(() => project.value?.review.annotations.filter((annotation) => annotation.status === \"open\").length ?? 0);\n  var CSS = `\n:root{font-family:Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;color:#e8edf7;background:#090d16;font-synthesis:none}\n*{box-sizing:border-box}\nbody{margin:0;min-width:320px;min-height:100vh;background:radial-gradient(circle at 30% -20%,#27365f 0,transparent 42%),#090d16;color:#e8edf7}\nbutton,input,textarea{font:inherit}\nbutton{border:1px solid #37445d;border-radius:9px;background:#182238;color:#e8edf7;padding:8px 13px;cursor:pointer}\nbutton:hover{background:#22304c}button:disabled{opacity:.5;cursor:default}.primary{background:#4d6df3;border-color:#6e87ff}.primary:hover{background:#5d7cff}\nbutton:focus-visible,input:focus-visible,textarea:focus-visible{outline:3px solid #8fa5ff;outline-offset:2px}\ninput,textarea{width:100%;border:1px solid #34415a;border-radius:9px;background:#0d1423;color:#f6f8fc;padding:9px 11px}\ntextarea{min-height:74px;resize:vertical}.app{min-height:100vh;display:grid;grid-template-columns:minmax(260px,320px) minmax(0,1fr)}\n.rail{border-right:1px solid #263149;background:rgba(9,13,22,.82);backdrop-filter:blur(20px);padding:24px;display:flex;flex-direction:column;gap:22px}\n.brand{display:flex;align-items:center;gap:11px}.mark{display:grid;place-items:center;width:34px;height:34px;border-radius:10px;background:linear-gradient(135deg,#7690ff,#9f70ff);font-weight:900;color:white;box-shadow:0 10px 30px #5f65ff44}\nh1{font-size:17px;margin:0}.eyebrow{font-size:11px;letter-spacing:.13em;text-transform:uppercase;color:#8fa5cf}.workspace{font-size:12px;color:#9eabc2;overflow-wrap:anywhere}\n.field{display:flex;flex-direction:column;gap:6px}.field>span{font-size:12px;font-weight:650;color:#b9c5d9}.actions{display:flex;gap:8px;flex-wrap:wrap}.rail-note{margin-top:auto;color:#7f8ba3;font-size:12px;line-height:1.5}\n.main{min-width:0;padding:28px 34px 52px}.topbar{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:22px}.topbar h2{font-size:25px;margin:2px 0 4px}.muted{color:#8795ae;font-size:13px}.statusline{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.badge{border:1px solid #33415c;border-radius:999px;padding:4px 9px;color:#afbdd5;font-size:12px}.badge.good{color:#a8edc1;border-color:#2f6d4b;background:#15362666}.badge.warn{color:#ffd79a;border-color:#775b31;background:#3a291766}\n.panel{background:#101725dd;border:1px solid #27334a;border-radius:14px;padding:18px;box-shadow:0 18px 70px #0005}.empty{max-width:650px;margin:14vh auto;text-align:center;padding:38px}.empty h2{font-size:28px;margin:0 0 10px}.empty p{color:#9aa7bd;line-height:1.6}\n.notice{margin:0 0 18px;padding:12px 14px;border-radius:10px;border:1px solid #34415a;background:#111a2b;color:#c4cee0}.notice.error{border-color:#78404c;background:#351a22;color:#ffc5cd}.notice.success{border-color:#347153;background:#173426;color:#bcf6d0}\n.issues{margin:9px 0 0;padding-left:20px}.issues code{color:#ffb5c1}.grid{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(250px,.8fr);gap:18px;margin-bottom:18px}.panel h3{margin:0 0 16px;font-size:14px;letter-spacing:.02em}.formgrid{display:grid;grid-template-columns:1fr 1fr;gap:13px}.wide{grid-column:1/-1}\n.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.metric{padding:13px;border:1px solid #29364e;border-radius:11px;background:#0c1320}.metric strong{display:block;font-size:22px}.metric span{font-size:11px;color:#8d9ab0;text-transform:uppercase;letter-spacing:.08em}\n.scene-list{display:flex;flex-direction:column;gap:11px}.scene{display:grid;grid-template-columns:40px minmax(0,1fr) auto;gap:13px;align-items:start;border:1px solid #29364e;border-radius:12px;background:#0c1320;padding:13px}.scene-no{display:grid;place-items:center;width:32px;height:32px;border-radius:9px;background:#1b2942;color:#b9c9e7;font-weight:750}.scene-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.scene-id{font:11px ui-monospace,SFMono-Regular,Menlo,monospace;color:#78869e}.scene-state{white-space:nowrap}\n@media(max-width:800px){.app{grid-template-columns:1fr}.rail{border-right:0;border-bottom:1px solid #263149}.main{padding:22px 18px 40px}.grid{grid-template-columns:1fr}.topbar{flex-direction:column}.formgrid{grid-template-columns:1fr}.wide{grid-column:auto}}\n`;\n  function sourceLabel(scene) {\n    if (scene.render.kind === \"composition\") return `Composition \\xB7 ${scene.render.composition.layers.length} layer${scene.render.composition.layers.length === 1 ? \"\" : \"s\"}`;\n    const recipe = scene.render.recipe;\n    if (recipe.capture != null) return `Capture \\xB7 ${recipe.capture.url ?? recipe.capture.file}`;\n    if (recipe.template != null) return `Template \\xB7 ${recipe.template}`;\n    if (recipe.cast != null) return `Terminal \\xB7 ${recipe.cast}`;\n    return `SVG \\xB7 ${recipe.svg}`;\n  }\n  function durationLabel(scene) {\n    const duration = scene.render.kind === \"composition\" ? scene.render.duration ?? scene.render.composition.duration : scene.render.recipe.duration;\n    return duration == null ? \"intrinsic timing\" : `${(duration / 1e3).toFixed(2)}s`;\n  }\n  function isGenerated(sceneId) {\n    return generation.value?.scenes.find((scene) => scene.id === sceneId)?.generated === true;\n  }\n  function render() {\n    const current = project.value;\n    return /* @__PURE__ */ jsx(Fragment, { children: [\n      /* @__PURE__ */ jsx(\"style\", { children: CSS }),\n      /* @__PURE__ */ jsx(\"div\", { class: \"app\", children: [\n        /* @__PURE__ */ jsx(\"aside\", { class: \"rail\", children: [\n          /* @__PURE__ */ jsx(\"div\", { class: \"brand\", children: [\n            /* @__PURE__ */ jsx(\"div\", { class: \"mark\", children: \"D\" }),\n            /* @__PURE__ */ jsx(\"div\", { children: [\n              /* @__PURE__ */ jsx(\"div\", { class: \"eyebrow\", children: \"Domotion\" }),\n              /* @__PURE__ */ jsx(\"h1\", { children: \"Studio\" })\n            ] })\n          ] }),\n          /* @__PURE__ */ jsx(\"div\", { class: \"field\", children: [\n            /* @__PURE__ */ jsx(\"span\", { children: \"Studio workspace\" }),\n            /* @__PURE__ */ jsx(\"div\", { class: \"workspace\", children: bootstrap.workspaceRoot })\n          ] }),\n          /* @__PURE__ */ jsx(\"label\", { class: \"field\", children: [\n            /* @__PURE__ */ jsx(\"span\", { children: \"Project file\" }),\n            /* @__PURE__ */ jsx(\"input\", { \"data-field\": \"project-path\", value: projectPath.value, spellcheck: \"false\", \"aria-label\": \"Project file\" })\n          ] }),\n          current == null && /* @__PURE__ */ jsx(\"label\", { class: \"field\", children: [\n            /* @__PURE__ */ jsx(\"span\", { children: \"New project title\" }),\n            /* @__PURE__ */ jsx(\"input\", { \"data-field\": \"new-title\", value: newTitle.value, \"aria-label\": \"New project title\" })\n          ] }),\n          /* @__PURE__ */ jsx(\"div\", { class: \"actions\", children: [\n            /* @__PURE__ */ jsx(\"button\", { class: \"primary\", \"data-action\": \"create\", disabled: busy.value, children: \"Create\" }),\n            /* @__PURE__ */ jsx(\"button\", { \"data-action\": \"open\", disabled: busy.value, children: \"Open\" })\n          ] }),\n          current != null && /* @__PURE__ */ jsx(\"div\", { class: \"actions\", children: [\n            /* @__PURE__ */ jsx(\"button\", { class: \"primary\", \"data-action\": \"save\", disabled: busy.value || !dirty.value, children: \"Save\" }),\n            /* @__PURE__ */ jsx(\"button\", { \"data-action\": \"reopen\", disabled: busy.value, children: \"Reopen\" })\n          ] }),\n          /* @__PURE__ */ jsx(\"p\", { class: \"rail-note\", children: \"Project JSON is the durable source. SVG and review video stay generated artifacts with revision provenance.\" })\n        ] }),\n        /* @__PURE__ */ jsx(\"main\", { class: \"main\", children: [\n          message.value !== \"\" && /* @__PURE__ */ jsx(\"div\", { class: `notice ${messageKind.value}`, role: \"status\", \"aria-live\": \"polite\", children: [\n            message.value,\n            issues.value.length > 0 && /* @__PURE__ */ jsx(\"ul\", { class: \"issues\", children: issues.value.map((issue) => /* @__PURE__ */ jsx(\"li\", { children: [\n              /* @__PURE__ */ jsx(\"code\", { children: issue.path }),\n              \": \",\n              issue.message\n            ] })) })\n          ] }),\n          current == null ? /* @__PURE__ */ jsx(\"section\", { class: \"panel empty\", children: [\n            /* @__PURE__ */ jsx(\"div\", { class: \"eyebrow\", children: \"A separate authoring workspace\" }),\n            /* @__PURE__ */ jsx(\"h2\", { children: \"Create or open a Studio project\" }),\n            /* @__PURE__ */ jsx(\"p\", { children: \"Shape the narrative and scenes here while SVG Scrubber remains a focused playback and review tool.\" })\n          ] }) : /* @__PURE__ */ jsx(Fragment, { children: [\n            /* @__PURE__ */ jsx(\"header\", { class: \"topbar\", children: [\n              /* @__PURE__ */ jsx(\"div\", { children: [\n                /* @__PURE__ */ jsx(\"div\", { class: \"eyebrow\", children: current.id }),\n                /* @__PURE__ */ jsx(\"h2\", { children: current.narrative.title }),\n                /* @__PURE__ */ jsx(\"div\", { class: \"muted\", children: projectPath.value })\n              ] }),\n              /* @__PURE__ */ jsx(\"div\", { class: \"statusline\", children: [\n                /* @__PURE__ */ jsx(\"span\", { class: `badge ${dirty.value ? \"warn\" : \"good\"}`, children: dirty.value ? \"Unsaved changes\" : \"Saved\" }),\n                /* @__PURE__ */ jsx(\"span\", { class: \"badge\", children: [\n                  \"Format v\",\n                  current.version\n                ] })\n              ] })\n            ] }),\n            /* @__PURE__ */ jsx(\"div\", { class: \"grid\", children: [\n              /* @__PURE__ */ jsx(\"section\", { class: \"panel\", children: [\n                /* @__PURE__ */ jsx(\"h3\", { children: \"Narrative\" }),\n                /* @__PURE__ */ jsx(\"div\", { class: \"formgrid\", children: [\n                  /* @__PURE__ */ jsx(\"label\", { class: \"field wide\", children: [\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Title\" }),\n                    /* @__PURE__ */ jsx(\"input\", { \"data-field\": \"narrative-title\", value: current.narrative.title })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"label\", { class: \"field wide\", children: [\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Summary\" }),\n                    /* @__PURE__ */ jsx(\"textarea\", { \"data-field\": \"narrative-summary\", children: current.narrative.summary ?? \"\" })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"label\", { class: \"field\", children: [\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Objective\" }),\n                    /* @__PURE__ */ jsx(\"textarea\", { \"data-field\": \"narrative-objective\", children: current.narrative.objective ?? \"\" })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"label\", { class: \"field\", children: [\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Audience\" }),\n                    /* @__PURE__ */ jsx(\"textarea\", { \"data-field\": \"narrative-audience\", children: current.narrative.audience ?? \"\" })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"label\", { class: \"field wide\", children: [\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Tone\" }),\n                    /* @__PURE__ */ jsx(\"input\", { \"data-field\": \"narrative-tone\", value: current.narrative.tone ?? \"\" })\n                  ] })\n                ] })\n              ] }),\n              /* @__PURE__ */ jsx(\"section\", { class: \"panel\", children: [\n                /* @__PURE__ */ jsx(\"h3\", { children: \"Generation & review\" }),\n                /* @__PURE__ */ jsx(\"div\", { class: \"metrics\", children: [\n                  /* @__PURE__ */ jsx(\"div\", { class: \"metric\", children: [\n                    /* @__PURE__ */ jsx(\"strong\", { children: current.scenes.length }),\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Scenes\" })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"div\", { class: \"metric\", children: [\n                    /* @__PURE__ */ jsx(\"strong\", { children: generation.value?.artifactCount ?? current.artifacts.length }),\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Artifacts\" })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"div\", { class: \"metric\", children: [\n                    /* @__PURE__ */ jsx(\"strong\", { children: openAnnotations.value }),\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Open notes\" })\n                  ] })\n                ] })\n              ] })\n            ] }),\n            /* @__PURE__ */ jsx(\"section\", { class: \"panel\", children: [\n              /* @__PURE__ */ jsx(\"h3\", { children: \"Story scenes\" }),\n              /* @__PURE__ */ jsx(\"div\", { class: \"scene-list\", children: current.scenes.map((scene, index) => /* @__PURE__ */ jsx(\"article\", { class: \"scene\", \"data-scene-id\": scene.id, children: [\n                /* @__PURE__ */ jsx(\"div\", { class: \"scene-no\", children: index + 1 }),\n                /* @__PURE__ */ jsx(\"div\", { children: [\n                  /* @__PURE__ */ jsx(\"label\", { class: \"field\", children: [\n                    /* @__PURE__ */ jsx(\"span\", { children: \"Scene title\" }),\n                    /* @__PURE__ */ jsx(\"input\", { \"data-field\": \"scene-title\", value: scene.title ?? \"\", \"aria-label\": `Scene ${index + 1} title` })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"div\", { class: \"scene-meta\", children: [\n                    /* @__PURE__ */ jsx(\"span\", { class: \"badge\", children: sourceLabel(scene) }),\n                    /* @__PURE__ */ jsx(\"span\", { class: \"badge\", children: durationLabel(scene) })\n                  ] }),\n                  /* @__PURE__ */ jsx(\"div\", { class: \"scene-id\", children: scene.id })\n                ] }),\n                /* @__PURE__ */ jsx(\"span\", { class: `badge scene-state ${isGenerated(scene.id) ? \"good\" : \"warn\"}`, children: isGenerated(scene.id) ? \"Generated\" : \"Needs generation\" })\n              ] })) })\n            ] })\n          ] })\n        ] })\n      ] })\n    ] });\n  }\n  var app = document.getElementById(\"app\");\n  if (app == null) throw new Error(\"Domotion Studio: missing #app\");\n  mount(app, render);\n  function setFailure(error) {\n    const fallback = error instanceof Error ? error.message : String(error);\n    const response = error;\n    issues.value = response.issues ?? [];\n    message.value = response.error ?? fallback;\n    messageKind.value = \"error\";\n  }\n  async function post(path, body) {\n    const response = await fetch(path, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\" },\n      body: JSON.stringify(body)\n    });\n    const result = await response.json();\n    if (!response.ok) throw result;\n    return result;\n  }\n  function acceptLoaded(result, action) {\n    projectPath.value = result.path;\n    project.value = result.project;\n    generation.value = result.generation;\n    dirty.value = false;\n    issues.value = [];\n    message.value = `${action} ${result.path}`;\n    messageKind.value = \"success\";\n  }\n  async function run(action) {\n    busy.value = true;\n    try {\n      await action();\n    } catch (error) {\n      setFailure(error);\n    } finally {\n      busy.value = false;\n    }\n  }\n  void delegate(app, \"click\", \"[data-action]\", (_event, target) => {\n    const action = target.dataset.action;\n    if (action === \"create\") {\n      void run(async () => acceptLoaded(await post(\"/api/create\", { path: projectPath.value, title: newTitle.value }), \"Created\"));\n    } else if (action === \"open\" || action === \"reopen\") {\n      void run(async () => acceptLoaded(await post(\"/api/open\", { path: projectPath.value }), action === \"reopen\" ? \"Reopened\" : \"Opened\"));\n    } else if (action === \"save\" && project.value != null) {\n      void run(async () => acceptLoaded(await post(\"/api/save\", { path: projectPath.value, project: project.value }), \"Saved\"));\n    }\n  });\n  void delegate(app, \"input\", \"[data-field]\", (_event, target) => {\n    const control = target;\n    const field = control.dataset.field;\n    if (field === \"project-path\") {\n      projectPath.value = control.value;\n      return;\n    }\n    if (field === \"new-title\") {\n      newTitle.value = control.value;\n      return;\n    }\n    const current = project.value;\n    if (current == null) return;\n    const next = structuredClone(current);\n    if (field === \"narrative-title\") next.narrative.title = control.value;\n    else if (field === \"narrative-summary\") next.narrative.summary = control.value || void 0;\n    else if (field === \"narrative-objective\") next.narrative.objective = control.value || void 0;\n    else if (field === \"narrative-audience\") next.narrative.audience = control.value || void 0;\n    else if (field === \"narrative-tone\") next.narrative.tone = control.value || void 0;\n    else if (field === \"scene-title\") {\n      const sceneId = control.closest(\"[data-scene-id]\")?.dataset.sceneId;\n      const scene = next.scenes.find((candidate) => candidate.id === sceneId);\n      if (scene != null) scene.title = control.value || void 0;\n    } else return;\n    project.value = next;\n    dirty.value = true;\n    issues.value = [];\n    message.value = \"\";\n  });\n})();\n";
