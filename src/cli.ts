/**
 * CLI for Domotion (DOM-to-animated-SVG renderer).
 *
 * Usage:
 *   npx tsx src/cli.ts <demo-script.ts>
 *
 * Demo scripts import from this package and use DemoRecorder + generateAnimatedSvg.
 */

console.log("Domotion — DOM-to-animated-SVG renderer");
console.log("Use DemoRecorder and generateAnimatedSvg in your demo scripts.");
console.log("Example: npx tsx examples/terminal-demo.ts");
