{
  "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
  "version": "2.1.0",
  "runs": [
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mstmy5weg7npr2py",
          "message": {
            "text": "The shared background/mask gradient path now resolves flat computed length-percentage calc() terms against the actual gradient reference length and fails closed on unsupported units. The Chromium image oracle exercises the full capture-to-SVG path and passes; focused unit coverage also proves raw em/rem no longer become numeric-prefix pixels.",
            "markdown": "The shared background/mask gradient path now resolves flat computed length-percentage calc() terms against the actual gradient reference length and fails closed on unsupported units. The Chromium image oracle exercises the full capture-to-SVG path and passes; focused unit coverage also proves raw em/rem no longer become numeric-prefix pixels."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/gradient-defs.ts"
                },
                "region": {
                  "startLine": 397,
                  "endLine": 433,
                  "snippet": {
                    "text": "/** Resolve the computed-value grammar Blink leaves at capture: px, %, exact\n * absolute units, or a flat calc() sum of those. Context-dependent units are\n * intentionally rejected because only Blink, in the source element's style\n * context, can resolve them correctly. */\nfunction parseResolvedLengthPercentage(token: string, basis: number): number | null {\n  const factor = (unit: string): number | null => {\n    if (unit === \"px\" || unit === \"\") return 1;\n    if (unit === \"pt\") return 96 / 72;\n    if (unit === \"pc\") return 16;\n    if (unit === \"in\") return 96;\n    if (unit === \"cm\") return 96 / 2.54;\n    if (unit === \"mm\") return 96 / 25.4;\n    if (unit === \"q\") return 96 / 101.6;\n    return null;\n  };\n  const resolveTerm = (raw: string): number | null => {\n    const m = /^([+-]?(?:\\d+(?:\\.\\d+)?|\\.\\d+))(%|px|pt|pc|in|cm|mm|q)?$/i.exec(raw.trim());\n    if (m == null) return null;\n    const value = Number(m[1]);\n    const unit = (m[2] ?? \"\").toLowerCase();\n    if (unit === \"%\") return basis > 0 ? value / 100 : null;\n    if (unit === \"\" && value !== 0) return null;\n    const f = factor(unit);\n    if (f == null || basis <= 0) return basis === 0 && f != null ? value * f : null;\n    return value * f / basis;\n  };\n  const calc = /^calc\\(\\s*(.*?)\\s*\\)$/i.exec(token);\n  if (calc == null) return resolveTerm(token);\n  const terms = calc[1].match(/[+-]?\\s*(?:\\d+(?:\\.\\d+)?|\\.\\d+)(?:%|px|pt|pc|in|cm|mm|q)?/gi);\n  if (terms == null || terms.join(\"\").replace(/\\s/g, \"\") !== calc[1].replace(/\\s/g, \"\")) return null;\n  let total = 0;\n  for (const term of terms) {\n    const value = resolveTerm(term.replace(/\\s/g, \"\"));\n    if (value == null) return null;\n    total += value;\n  }\n  return total;"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "proof"
            ],
            "ext-ai-tool-confidence": 0.98
          },
          "relatedLocations": [
            {
              "id": 0,
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tests/gradient-length-units.e2e.test.ts"
                },
                "region": {
                  "startLine": 23
                }
              }
            },
            {
              "id": 1,
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/gradients.test.ts"
                },
                "region": {
                  "startLine": 107
                }
              }
            }
          ],
          "rank": 85,
          "workItemUris": [
            "DM-2194"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "9c0c6abf244c00f24801d04b551c24b1"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/domotion.git",
          "revisionId": "4c7a7f0a2da48e7ceacf4ebc78ad135b392a0908",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "msydau78t9kzllsh",
          "message": {
            "text": "DM-2290: A repeating radial period must define the SVG radial vector, not merely occupy the first fraction of a full-radius vector. CSS measures radial stops along the computed x-radius; after resolving that radius, Domotion maps the first/last stop to SVG fr/r and normalizes the period to 0..1, exactly paralleling its linear endpoint construction. SVG then repeats the period inward/outward. Degenerate periods are collapsed before four-decimal serialization so a near-zero CSS period cannot become an accidental zero-width SVG vector.",
            "markdown": "DM-2290: A repeating radial period must define the SVG radial vector, not merely occupy the first fraction of a full-radius vector. CSS measures radial stops along the computed x-radius; after resolving that radius, Domotion maps the first/last stop to SVG fr/r and normalizes the period to 0..1, exactly paralleling its linear endpoint construction. SVG then repeats the period inward/outward. Degenerate periods are collapsed before four-decimal serialization so a near-zero CSS period cannot become an accidental zero-width SVG vector."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/gradient-defs.ts"
                },
                "region": {
                  "startLine": 337,
                  "endLine": 385,
                  "snippet": {
                    "text": "\n  // A radial percentage/length stop is measured on the gradient ray, whose\n  // end point is the positive-x intersection of the ending shape: `rx` CSS\n  // pixels. The previous half-diagonal estimate was never corrected after the\n  // real shape was known, so px periods were normalized against a different\n  // line from the one SVG painted (especially visible in non-square boxes).\n  let emitStops = parseGradientStops(parts.slice(stopsStart), rx);\n  if (emitStops.length === 0) return \"\";\n\n  // Mirror the linear-gradient construction: one repeating CSS period becomes\n  // the SVG radial vector, and SVG repeats outside it. Leaving a 20px period as\n  // stops 0..0.1 on a 200px radius makes SVG pad the final color over the other\n  // 90%, which is the nearly-solid rectangle DM-2290 exposed. `fr` is the\n  // radial analogue of moving a linear gradient's first endpoint; it preserves\n  // a positive first-stop offset and lets spreadMethod repeat inward as well as\n  // outward. Degenerate periods become the final stop's solid color, matching\n  // CSS Images' fix-up rather than inventing a minimum one-pixel ring.\n  let innerR = 0;\n  let outerR = rx;\n  let useRepeat = repeating;\n  if (repeating && emitStops.length >= 2) {\n    const firstPos = emitStops[0].pos;\n    const lastPos = emitStops[emitStops.length - 1].pos;\n    const period = lastPos - firstPos;\n    // `stopFmt` emits four fractional digits; periods smaller than half that\n    // precision would serialize both radii identically and become a zero-width\n    // SVG vector. Treat that as the CSS degenerate-period case deliberately.\n    if (period * rx > 5e-5) {\n      innerR = Math.max(0, firstPos * rx);\n      outerR = Math.max(innerR + Number.EPSILON, lastPos * rx);\n      emitStops = emitStops.map((s) => ({ ...s, pos: (s.pos - firstPos) / period }));\n    } else {\n      const solid = emitStops[emitStops.length - 1];\n      emitStops = [{ ...solid, pos: 0 }, { ...solid, pos: 1 }];\n      useRepeat = false;\n    }\n  }\n\n  const spread = useRepeat ? ` spreadMethod=\"repeat\"` : \"\";\n  const stopsMarkup = normalizeTransparentStops(emitStops).map((s) => `<stop offset=\"${stopFmt(s.pos)}\" stop-color=\"${colorStr(s.color)}\" />`).join(\"\");\n\n  // SVG radialGradient has a single r — use rx as r and scale Y via gradientTransform\n  // to stretch it into an ellipse matching (rx, ry).\n  const rScale = rx > 0 ? ry / rx : 1;\n  const gradientTransform = Math.abs(rScale - 1) > 0.001\n    ? ` gradientTransform=\"translate(0 ${stopFmt(cy * (1 - rScale))}) scale(1 ${stopFmt(rScale)})\"`\n    : \"\";\n\n  const focal = innerR > 0"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ],
            "ext-ai-tool-confidence": 0.97
          },
          "workItemUris": [
            "DM-2290"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "8962bb61efb2b033d11e78f17d6ae708"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/domotion.git",
          "revisionId": "5076f177b3eee2f3a2792f554d40eba81c753cc0",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "msz9zth0ti2xv19a",
          "message": {
            "text": "Corner keywords intentionally use Blink’s magic-corner bearing, not a direct vector to the named corner. Chromium revision 7d859f27 computes 90 - atan2(width,height) before endpoint projection; the generated oracle independently pins all four directions over three non-square boxes and its mutation control rejects the retired direct-corner rule.",
            "markdown": "Corner keywords intentionally use Blink’s magic-corner bearing, not a direct vector to the named corner. Chromium revision 7d859f27 computes `90 - atan2(width,height)` before endpoint projection; the generated oracle independently pins all four directions over three non-square boxes and its mutation control rejects the retired direct-corner rule."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/gradient-defs.ts"
                },
                "region": {
                  "startLine": 601,
                  "endLine": 625,
                  "snippet": {
                    "text": "/** Map 'to top', 'to right', 'to top right' etc. to a CSS gradient angle (deg).\n *\n * Corner directions use Blink's “magic corner” construction. The gradient\n * line is perpendicular to the center-to-corner diagonal so its 50% line\n * reaches the other two corners. `CSSLinearGradientValue::CreateGradient`\n * computes `90 - atan2(width, height)` before `EndPointsFromAngle` (Chromium\n * rev 7d859f27, css_gradient_value.cc:1410-1430). A 3:1 landscape box's\n * `to top right` angle is therefore ~18°, not the direct-to-corner ~72°.\n */\nfunction cssDirectionToAngle(dir: string, w: number = 1, h: number = 1): number {\n  const parts = dir.trim().toLowerCase().split(/\\s+/);\n  const set = new Set(parts);\n  const hasTop = set.has(\"top\");\n  const hasBottom = set.has(\"bottom\");\n  const hasLeft = set.has(\"left\");\n  const hasRight = set.has(\"right\");\n  if (hasTop && !hasLeft && !hasRight) return 0;\n  if (hasBottom && !hasLeft && !hasRight) return 180;\n  if (hasRight && !hasTop && !hasBottom) return 90;\n  if (hasLeft && !hasTop && !hasBottom) return 270;\n  const cornerAngle = 90 - Math.atan2(w, h) * 180 / Math.PI;\n  if (hasTop && hasRight) return cornerAngle;\n  if (hasBottom && hasRight) return 180 - cornerAngle;\n  if (hasBottom && hasLeft) return 180 + cornerAngle;\n  if (hasTop && hasLeft) return 360 - cornerAngle;"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ],
            "ext-ai-tool-confidence": 0.99
          },
          "relatedLocations": [
            {
              "id": 0,
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tools/paint-geometry-oracle.ts"
                },
                "region": {
                  "startLine": 50
                }
              }
            }
          ],
          "rank": 95,
          "workItemUris": [
            "DM-2297"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "1f122784728855bb8bacb606fb4c389d"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/domotion.git",
          "revisionId": "147cbc508abdb5b54cf44fa9ab651bfbd47bd166",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszakp0husrhvmmg",
          "message": {
            "text": "Color hints now copy Blink’s own nine-stop approximation exactly. The previous seven-stop uniform-weight sampler was a Domotion-specific visual fit; Chromium revision 7d859f27 deliberately places two stops on the short side and seven on the long side before applying the power weighting, so matching those positions is the logical parity rule.",
            "markdown": "Color hints now copy Blink’s own nine-stop approximation exactly. The previous seven-stop uniform-weight sampler was a Domotion-specific visual fit; Chromium revision 7d859f27 deliberately places two stops on the short side and seven on the long side before applying the power weighting, so matching those positions is the logical parity rule."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/gradient-defs.ts"
                },
                "region": {
                  "startLine": 541,
                  "endLine": 599,
                  "snippet": {
                    "text": "  // Blink replaces every interpolation hint with exactly nine ordinary stops\n  // (`ReplaceColorHintsWithColorStops`, css_gradient_value.cc:266-399,\n  // Chromium rev 7d859f27). Mirror its deliberately asymmetric placement:\n  // two samples on the short side and seven on the long side. This is itself\n  // Blink's piecewise-linear approximation of the CSS power curve, so copying\n  // it is exact renderer logic rather than choosing our own sampling quality.\n  if (hints.length > 0) {\n    const out: GradientStop[] = [];\n    let hintIdx = 0;\n    for (let s = 0; s < stops.length; s++) {\n      out.push(stops[s]);\n      // Is there a hint between this color's last stop and the next color's first stop?\n      if (s === stops.length - 1) continue;\n      const thisColorIdx = stopColorIdx[s];\n      const nextColorIdx = stopColorIdx[s + 1];\n      if (thisColorIdx === nextColorIdx) continue; // inside same color (hard stop)\n      while (hintIdx < hints.length && hints[hintIdx].afterColorIdx <= thisColorIdx) {\n        const h = hints[hintIdx++];\n        if (h.afterColorIdx !== thisColorIdx) continue;\n        const a = stops[s];\n        const b = stops[s + 1];\n        const span = b.pos - a.pos;\n        if (span <= 0) continue;\n        const hRel = (h.pos - a.pos) / span;\n        const ca = a.color, cb = b.color;\n        const sameColor = ca.r === cb.r && ca.g === cb.g && ca.b === cb.b && ca.a === cb.a;\n        if (sameColor || Math.abs(hRel - 0.5) < 1e-6) continue;\n        if (hRel <= 1e-6) {\n          out.push({ color: { ...cb }, pos: a.pos });\n          continue;\n        }\n        if (hRel >= 1 - 1e-6) {\n          out.push({ color: { ...ca }, pos: b.pos });\n          continue;\n        }\n        const expo = Math.log(0.5) / Math.log(hRel);\n        const leftDist = h.pos - a.pos;\n        const rightDist = b.pos - h.pos;\n        const positions: number[] = [];\n        if (leftDist > rightDist) {\n          for (let y = 0; y < 7; y++) positions.push(a.pos + leftDist * ((7 + y) / 13));\n          positions.push(h.pos + rightDist / 3, h.pos + rightDist * 2 / 3);\n        } else {\n          positions.push(a.pos + leftDist / 3, a.pos + leftDist * 2 / 3);\n          for (let y = 0; y < 7; y++) positions.push(h.pos + rightDist * (y / 13));\n        }\n        for (const pos of positions) {\n          const t = (pos - a.pos) / span;\n          const w = Math.pow(t, expo);\n          out.push({\n            color: {\n              r: Math.round(ca.r + (cb.r - ca.r) * w),\n              g: Math.round(ca.g + (cb.g - ca.g) * w),\n              b: Math.round(ca.b + (cb.b - ca.b) * w),\n              a: ca.a + (cb.a - ca.a) * w,\n            },\n            pos,\n          });\n        }"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ],
            "ext-ai-tool-confidence": 0.99
          },
          "relatedLocations": [
            {
              "id": 0,
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tools/paint-geometry-oracle.ts"
                },
                "region": {
                  "startLine": 73
                }
              }
            }
          ],
          "rank": 94,
          "workItemUris": [
            "DM-2305"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "3d1b4bf98c6ba0968351f44259dd1a09"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/domotion.git",
          "revisionId": "c7700f04986f7e7d34b9b26ae2db79763a90ffc0",
          "branch": "main"
        }
      ]
    }
  ]
}
