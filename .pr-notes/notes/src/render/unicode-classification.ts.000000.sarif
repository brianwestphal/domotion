{
  "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
  "version": "2.1.0",
  "runs": [
    {
      "tool": {
        "driver": {
          "name": "Codex",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mswtu17tza55lfgg",
          "message": {
            "text": "Supported helper-backed classification now calls the same ICU operations Chromium calls. The existing JavaScript regexes remain only as the nonfatal degraded path; the focused text and Windows fallback suites pass with the pinned ICU 78.2 helper and Chromium icudtl.dat.",
            "markdown": "Supported helper-backed classification now calls the same ICU operations Chromium calls. The existing JavaScript regexes remain only as the nonfatal degraded path; the focused text and Windows fallback suites pass with the pinned ICU 78.2 helper and Chromium icudtl.dat."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/unicode-classification.ts"
                },
                "region": {
                  "startLine": 168,
                  "endLine": 191,
                  "snippet": {
                    "text": "  const icu = icuCodepointProperties(cp);\n  // U_CONTROL_CHAR, U_SPACE_SEPARATOR, U_LINE_SEPARATOR,\n  // U_PARAGRAPH_SEPARATOR from the Chromium-pinned ICU headers.\n  if (icu != null && (icu.generalCategory === 15 ||\n      (icu.generalCategory >= 12 && icu.generalCategory <= 14))) return true;\n  let s: string;\n  try { s = String.fromCodePoint(cp); } catch { return false; }\n  if (INKLESS_CATEGORY_RE.test(s)) return true;\n  return isHarfbuzzDefaultIgnorable(cp);\n}\n\n// The Unicode `Ideographic` binary property, exactly as Blink consults it:\n// `Character::IsIdeographic` is `u_hasBinaryProperty(c, UCHAR_IDEOGRAPHIC)`\n// (external/chromium third_party/blink/renderer/platform/text/character.h:106-108,\n// rev 7d859f27; identical at shipping tag 147.0.7727.15). JS regex `\\p{Ideographic}`\n// is the same UCD property, so this is a transcription, not an approximation.\n// It gates Blink's macOS per-character fallback cache: caching happens ONLY for\n// [:Ideographic=Yes:] codepoints (mac/font_cache_mac.mm:335-347).\nconst IDEOGRAPHIC_RE = /^\\p{Ideographic}$/u;\nexport function isIdeographicCp(cp: number): boolean {\n  const icu = icuCodepointProperties(cp);\n  if (icu != null) return (icu.binaryProperties & ICU_BINARY.IDEOGRAPHIC) !== 0;\n  let s: string;\n  try { s = String.fromCodePoint(cp); } catch { return false; }"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "proof"
            ],
            "ext-ai-tool-confidence": 0.95
          },
          "relatedLocations": [
            {
              "id": 0,
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tools/icu-helper/src/main.cpp"
                },
                "region": {
                  "startLine": 110
                }
              }
            }
          ],
          "rank": 95,
          "workItemUris": [
            "DM-2254"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "e919eaf35f127a4ee9a3cc65575bcc12"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/domotion.git",
          "revisionId": "19930b64b2e421f81b4a0f8a95e222f01d1b7bcc",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mt2u4742ab14i9we",
          "message": {
            "text": "This lookup mirrors Blink's compact operator dictionary instead of expanding a visually curated set. It preserves the special UTF-16 branches, two-ASCII remapping, overlay inheritance, form bits, compact ranges, and category metadata from Chromium 7d859f27. The fence and separator fields are explicitly false because MathML Core and Blink's dictionary property table default only stretch, symmetry, large-op, movable-limits, and spacing; author attributes own the legacy booleans.",
            "markdown": "This lookup mirrors Blink's compact operator dictionary instead of expanding a visually curated set. It preserves the special UTF-16 branches, two-ASCII remapping, overlay inheritance, form bits, compact ranges, and category metadata from Chromium 7d859f27. The fence and separator fields are explicitly false because MathML Core and Blink's dictionary property table default only stretch, symmetry, large-op, movable-limits, and spacing; author attributes own the legacy booleans."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/unicode-classification.ts"
                },
                "region": {
                  "startLine": 1020,
                  "endLine": 1064,
                  "snippet": {
                    "text": "function blinkMathMLOperatorCategory(content: string, form: MathMLOperatorForm): MathMLOperatorCategory {\n  let key = 0;\n  const scalars = [...content];\n  if (content.length === 1) {\n    const cp = content.charCodeAt(0);\n    if (cp < 0x0320 || cp > 0x03ff) key = cp;\n  } else if (content.length === 2) {\n    const cp = content.codePointAt(0)!;\n    if (cp === 0x1eef0 || cp === 0x1eef1) return form === 'postfix' ? 'I' : 'none';\n    if (content.charCodeAt(1) === 0x0338 || content.charCodeAt(1) === 0x20d2) key = content.charCodeAt(0);\n    else if (scalars.length === 2) {\n      const index = TWO_ASCII_OPERATORS.indexOf(content as typeof TWO_ASCII_OPERATORS[number]);\n      if (index >= 0) key = 0x0320 + index;\n    }\n  }\n  if (key === 0) return 'none';\n  if (form === 'infix' && (key === 0x7c || key === 0x223c)) return 'force-default';\n  if (form === 'prefix' && ((key >= 0x2145 && key <= 0x2146) || key === 0x2202 || (key >= 0x221a && key <= 0x221c))) return 'L';\n  if (form === 'infix' && (key === 0x2c || key === 0x3a || key === 0x3b)) return 'M';\n  if (key >= 0x2000 && key <= 0x2bff) key -= 0x1c00;\n  else if (key > 0x03ff) return 'none';\n  if (form === 'prefix') key |= 0x1000;\n  else if (form === 'postfix') key |= 0x2000;\n  let found: readonly [number, number] | undefined;\n  for (const range of BLINK_MATHML_COMPACT_DICTIONARY) {\n    if ((range[0] & 0x3fff) > key) break;\n    found = range;\n  }\n  if (found == null || key > ((found[0] & 0x3fff) + found[1])) return 'none';\n  const encoded = found[0] >>> 12;\n  if (encoded === 0) return 'A';\n  if (encoded === 4) return 'B';\n  if (encoded === 8) return 'C';\n  if (encoded === 1 || encoded === 2 || encoded === 12) return 'D/E/K';\n  if (encoded === 5 || encoded === 6) return 'F/G';\n  if (encoded === 9) return 'H';\n  if (encoded === 10) return 'I';\n  return 'J';\n}\n\n/** Exact transcription of Blink's compact MathML operator dictionary lookup. */\nexport function mathMLOperatorDictionaryEntry(content: string, form: MathMLOperatorForm): MathMLOperatorDictionaryEntry {\n  const category = blinkMathMLOperatorCategory(content, form);\n  return { form, category, ...CATEGORY_PROPERTIES[category] };\n}"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ],
            "ext-ai-tool-confidence": 0.99
          },
          "rank": 100,
          "workItemUris": [
            "DM-2397"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "8f809fdd75d1b25b2aac0cc384c94c4c"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/domotion.git",
          "revisionId": "d1b818ba38a31e9fc2a8195c979876414009e6c0",
          "branch": "main"
        }
      ]
    }
  ]
}
