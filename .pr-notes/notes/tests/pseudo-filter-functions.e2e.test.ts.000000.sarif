{
  "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
  "version": "2.1.0",
  "runs": [
    {
      "tool": {
        "driver": {
          "name": "Codex",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mt3n9xrbw2hpz000",
          "message": {
            "text": "The independent Chromium oracle rejects both primitive lowering and a missing-filter mutation through the real img-rendered SVG consumer path. It covers all ten shorthand functions, reversed non-commutative lists, identity/none, transform/origin, ancestor clipping, moving-pixel bounds, zoom, and DPR 1/2; the production result stays under MAE 2.2 and below 35% of the filter-stripped mutation error. The pure box matrix also asserts no raster image and no feGaussianBlur. The shared representation oracle reports raster boundary oracle: 28/28; mutation control moved, including the new pseudo shorthand-list vector negative, proving the intended native route in the shared wrapper (0).",
            "markdown": "The independent Chromium oracle rejects both primitive lowering and a missing-filter mutation through the real img-rendered SVG consumer path. It covers all ten shorthand functions, reversed non-commutative lists, identity/none, transform/origin, ancestor clipping, moving-pixel bounds, zoom, and DPR 1/2; the production result stays under MAE 2.2 and below 35% of the filter-stripped mutation error. The pure box matrix also asserts no raster image and no feGaussianBlur. The shared representation oracle reports `raster boundary oracle: 28/28; mutation control moved`, including the new pseudo shorthand-list vector negative, proving the intended native route in [the shared wrapper](0)."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tests/pseudo-filter-functions.e2e.test.ts"
                },
                "region": {
                  "startLine": 142,
                  "endLine": 199,
                  "snippet": {
                    "text": "  it(\"captures every shorthand function and emits the computed lists verbatim\", async () => {\n    const page = await env!.browser.newPage({ viewport: { width: W, height: H }, deviceScaleFactor: 1 });\n    try {\n      await page.setContent(HTML);\n      const computed = await page.locator(\".card\").evaluateAll((cards) =>\n        cards.map((card) => getComputedStyle(card, \"::before\").filter),\n      );\n      const tree = await captureElementTree(page, \"body\", { x: 0, y: 0, width: W, height: H });\n      const captured = pseudoBoxes(tree).map((box) => box.filter).filter((value): value is string => value != null);\n      for (const filter of computed) expect(captured).toContain(filter);\n\n      const svg = elementTreeToSvg(tree, W, H);\n      for (const filter of computed) expect(svg).toContain(`style=\"filter:${filter}\"`);\n      expect(svg).not.toContain(\"<feGaussianBlur\");\n      expect(svg).not.toContain(\"<image\");\n      expect(svg).toContain('style=\"filter:blur(0px)\"');\n      expect(svg).not.toContain('style=\"filter:none\"');\n\n      const forward = computed[10];\n      const reverse = computed[11];\n      expect(forward).not.toBe(reverse);\n      expect(svg.indexOf(`style=\"filter:${forward}\"`)).toBeLessThan(\n        svg.indexOf(`style=\"filter:${reverse}\"`),\n      );\n      expect(svg).toMatch(/transform=\"translate\\([^\\\"]+\\) matrix\\([^\\\"]+\\) translate\\([^\\\"]+\\)\"><g style=\"filter:opacity\\(0\\.35\\) drop-shadow/);\n    } finally {\n      await page.close();\n    }\n  }, 60_000);\n\n  it(\"beats a filter-stripped mutation across DPR while preserving zoom, transform, clipping, and moving-pixel regions\", async () => {\n    for (const dpr of [1, 2]) {\n      const context = await env!.browser.newContext({ viewport: { width: W, height: H }, deviceScaleFactor: dpr });\n      const source = await context.newPage();\n      const rendered = await context.newPage();\n      try {\n        await source.setContent(HTML);\n        const expected = await source.screenshot({ type: \"png\" });\n        const tree = await captureElementTree(source, \"body\", { x: 0, y: 0, width: W, height: H });\n        const exactSvg = elementTreeToSvg(tree, W, H);\n        await rendered.setContent(`<body style=\"margin:0\">${exactSvg}</body>`, { waitUntil: \"load\" });\n        const actual = await rendered.screenshot({ type: \"png\" });\n\n        const mutation = structuredClone(tree);\n        stripPseudoFilters(mutation);\n        const mutationSvg = elementTreeToSvg(mutation, W, H);\n        await rendered.setContent(`<body style=\"margin:0\">${mutationSvg}</body>`, { waitUntil: \"load\" });\n        const mutated = await rendered.screenshot({ type: \"png\" });\n\n        const exactError = await meanAbsoluteError(expected, actual);\n        const mutationError = await meanAbsoluteError(expected, mutated);\n        expect(exactError, `DPR ${dpr} exact MAE`).toBeLessThan(2.2);\n        expect(exactError, `DPR ${dpr} mutation discriminator`).toBeLessThan(mutationError * 0.35);\n      } finally {\n        await context.close();\n      }\n    }\n  }, 120_000);"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "test-evidence"
            ],
            "ext-ai-tool-confidence": 0.97
          },
          "relatedLocations": [
            {
              "id": 0,
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/render/pseudo-filter.ts"
                },
                "region": {
                  "startLine": 44
                }
              }
            }
          ],
          "rank": 90,
          "workItemUris": [
            "DM-2367"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "1acd43489e67239fb4210ed44300037d"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/domotion.git",
          "revisionId": "bbd91e32ad53a83e1873e601df23a4936b28c86b",
          "branch": "main"
        }
      ]
    }
  ]
}
