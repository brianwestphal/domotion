# Domotion docs

Requirements / design docs for the rendering pipeline. Numbered to match the order they were authored; numbering does not imply dependency order.

> **Cross-platform note**: macOS, Linux, and Windows are first-class supported capture platforms. The rendering and font-fallback pipelines are calibrated against Chromium's native platform path (CoreText, fontconfig, and DirectWrite respectively). macOS remains the primary development and testing platform, but that testing priority is not a separate support tier: platform-specific defects on Linux or Windows are supported defects, and changes that affect them require native validation.

| Doc | Topic |
|---|---|
| [01-fidelity.md](01-fidelity.md) | Overall rendering-fidelity contract: what CSS features round-trip, what's partial, what isn't supported. The big-picture support matrix. |
| [02-writing-mode.md](02-writing-mode.md) | CSS `writing-mode` (vertical-rl / vertical-lr / sideways-*), rendered as scalable glyphs with Blink-equivalent line-relative transforms. |
| [03-font-family-chain.md](03-font-family-chain.md) | Honoring author-specified font-family chains (e.g. `"Helvetica Neue", "Times New Roman", monospace`). |
| [04-input-pseudos.md](04-input-pseudos.md) | Author-styled `<input>` shadow-DOM pseudos (range track/thumb, checkbox/radio, color swatch, etc.). |
| [05-progress-meter-pseudos.md](05-progress-meter-pseudos.md) | Author-styled `<progress>` and `<meter>` pseudos from Blink's resolved UA-shadow styles. |
| [06-css-transforms.md](06-css-transforms.md) | CSS 2D `transform` (rotate / scale / skew / matrix) on rendered SVG groups. |
| [07-gradient-fills.md](07-gradient-fills.md) | CSS `linear-gradient` / `radial-gradient` → SVG `<linearGradient>` / `<radialGradient>` with px-positioned stops. |
| [08-animation-model.md](08-animation-model.md) | Animated-SVG composition: transitions (`crossfade` / `push-left` / `scroll` / `cut`), per-frame overlays, intra-frame property animations, frame-local SVG overlays, slide-in entrance sugar. |
| [09-vertical-range.md](09-vertical-range.md) | Vertical-axis `<input type=range>` via `writing-mode: vertical-*` + `direction` semantics. |
| [10-repeating-gradients.md](10-repeating-gradients.md) | `repeating-linear-gradient` / `repeating-radial-gradient` and `calc(N% ± Mpx)` stop positions. |
| [11-custom-checkbox-radio.md](11-custom-checkbox-radio.md) | **Source-owned checkable indicators** (DM-2459) — exact `appearance:none` `::before`/`::after` and `appearance:base` `::checkmark` capture/paint, authoritative-empty synthesis suppression, old-tree-only compatibility, and native three-OS DPR gate. |
| [12-diff-scoring.md](12-diff-scoring.md) | `tests/html-test-suite.tsx` visual-diff metric — Yee anti-aliasing filter, threshold rationale, diff-image legend. |
| [13-cursor-overlay.md](13-cursor-overlay.md) | Cursor / touch / click overlay simulation API — proposed design, awaiting feedback (DM-277). |
| [14-per-corner-border-radius.md](14-per-corner-border-radius.md) | Per-corner border-radius (asymmetric `10px 30px 50px 70px` and elliptical `50px / 20px`) — capture format, corner-overlap clamp, inner-radius derivation, path geometry. |
| [16-coretext-glyph-extraction.md](16-coretext-glyph-extraction.md) | Native glyph-outline extraction via CoreText / Pango / DirectWrite for fonts whose outlines fontkit can't parse (PingFang's `hvgl`, Apple-private formats). Helper-binary architecture, IPC protocol, cross-platform plan. |
| [17-replaced-element-snapshots.md](17-replaced-element-snapshots.md) | Static-snapshot rasterization for `<canvas>` / `<video>` / `<iframe>` / `<object>` / `<embed>` — hide-everything-else stylesheet + per-element `page.screenshot` so replaced elements no longer leave blank holes. |
| [26-self-contained-svgs.md](26-self-contained-svgs.md) | `embedRemoteImages(tree)` pre-pass that fetches every http(s) image URL referenced by the captured tree and inlines the bytes as `data:` URIs. Required for SVGs that need to load in macOS Preview / QuickLook / Finder thumbnails (which don't fetch remote resources from local files). Opt-in via `selfContained: true` in `CaptureOptions`; demos run it unconditionally. |
| [27-image-resize-on-embed.md](27-image-resize-on-embed.md) | `resizeEmbeddedImages` pre-pass that downscales each inlined image to its render rect × `hiDPIFactor` (default 2×) and re-encodes as PNG. Targets 50–80 % SVG size reduction on news-site captures with no visible diff at the captured viewport. Opt-in via `embedRemoteImagesResize: true`; only runs when `selfContained: true` has populated the data-URI cache. |
| [28-conic-gradient.md](28-conic-gradient.md) | `conic-gradient` / `repeating-conic-gradient` background layers via a pattern-raster pre-pass (sharp → PNG → `<pattern><image>`). Covers `from <angle>`, `at <position>`, hard stops, multi-layer composition. Animation deferred. |
| [29-dark-mode.md](29-dark-mode.md) | Dark-mode rendering: caller-chosen `colorScheme`, emitted SVG metadata, and capture-side `rootBgComputed`; the historical sampled form-control palette was retired by DM-2458 in favor of captured Chromium native surfaces. |
| [25-keyframe-extraction-for-symbols.md](25-keyframe-extraction-for-symbols.md) | t=0 paint-state baking for CSS-animated `<symbol>` contents — `getComputedStyle().transform` (composed with `transform-origin`) and presentation attrs are baked onto the inlined subtree at capture time. Replaces DM-499's animated-subtree raster fallback. The animation itself doesn't survive; the captured frame matches Chrome's paint. |
| [24-inline-svg-icon-references.md](24-inline-svg-icon-references.md) | Inline SVG icons that use `<use href="#id">` to reference symbols defined in a separate hidden `<defs>` SVG (apple.com country dropdown checkmark, social icons, etc.) — resolve-and-inline at capture time, rasterize fallback when refs can't be resolved. |
| [22-mask-element-paint-references.md](22-mask-element-paint-references.md) | CSS `mask-image: element(#id)` paint references — per-id `page.screenshot` of the referenced element, emitted as `<image>` inside `<mask>` with `mask-type="luminance"` for match-source. Same-document only; deduped per id. |
| [18-background-clip-text.md](18-background-clip-text.md) | CSS `background-clip: text` — complete gradient/URL/bottom-color background stack through a Blink-equivalent alpha glyph mask, followed by ordinary vector foreground fill/stroke; includes descendant and fragmented-inline ownership. |
| [23-css-sprite-icons.md](23-css-sprite-icons.md) | CSS sprite icons (image-replacement idiom: `text-indent: -9999px; overflow: hidden; background: url(/sprite.png) -X -Y no-repeat`) — replaced-element raster path captures the painted icon rect, with off-screen text suppressed and `aria-label` carried through as SVG `<title>`. |
| [33-element-out-of-viewbox-hiding.md](33-element-out-of-viewbox-hiding.md) | Phase 2 of DM-599 animation performance work — element-level `display: none` for content outside the viewBox. Static off-viewBox elements get a direct `style="display:none"`; animated elements crossing the viewBox boundary follow the hide-only-before/after rule from DM-599 feedback. Composition-time pass driven by `CapturedElement.{x,y,width,height}` + the intra-frame animation's explicit `from`/`to`. |
| [32-svg-optimization.md](32-svg-optimization.md) | SVG size reduction — `optimizeSvg(svg)` (SVGO post-pass, plugin set tuned for path-mode text) and `gzipSvg(svg)` (zlib gzip for `.svgz` output). CLI auto-detects `.svgz` from `-o` and implies `--optimize` unless `--no-optimize` is passed. |
| [31-region-feedback.md](31-region-feedback.md) | Region-scoped feedback in the demos-review tool — user drags rectangles on the `expected`/`actual`/`diff` triplet shown by `npm run demos:review`; rectangles are serialized into a `REGIONS:` block in the Hot Sheet ticket comment; the AI iteration loop produces one crop per rectangle (under `tests/output/region-crops/DM-{id}/{noteId}/`) and feeds them as primary visual evidence alongside the full attachments. Contract for DM-570; implementation tickets filed separately. |
| [37-scroll-pattern-grammar.md](37-scroll-pattern-grammar.md) | Canonical EBNF + semantics reference for the scroll-pattern language: top-level sequencing, parenthesised groups, `until` clauses, direction/axis resolution, speed-vs-duration semantics (and the CLI flags / config keys / programmatic options that drive `defaultSpeed`), easing, and worked examples. The parser (`src/scroll/pattern.ts`) and executor (`src/scroll/executor.ts`) both point here. |
| [39-clip-path-fragment-references.md](39-clip-path-fragment-references.md) | **URL clip-path reference ownership** — Blink-exclusive URL grammar, HTML border/object-bounding-box mapping, SVG-child forced fill-box behavior, same-document/nested-frame/external capture, and strict fail-closed controls. |
| [46-cli-npx-invocation.md](46-cli-npx-invocation.md) | Contract for running the CLI as an npm bin (`npx -p domotion-svg domotion …`): the `bin` mapping + shebang, why the package's five bins mean the bin must be named explicitly, build-before-publish, runtime version read from `package.json` (no hardcoded literal), Node 22 + Playwright-Chromium prerequisites (auto-installed on first run), and the unsupported git-URL-invocation caveat. |
| [47-svg-to-video.md](47-svg-to-video.md) | The standalone `svg-to-video` CLI (second `bin`): render an animated SVG to video via Playwright frame-stepping (WAAPI `getAnimations()` currentTime sampling for frame-accurate timing — `domotion animate` SVGs and SMIL; JS-`rAF` out of scope) piped to ffmpeg. Covers fps/size/format/container, supersampling, background-music / foreground-audio / caption muxing, the hard ffmpeg requirement + install guidance, and the disk-space pre-flight. |
| [48-har-source.md](48-har-source.md) | **DRAFT** — accept a `.har` file as a `domotion capture` source: replay recorded responses offline/deterministically via Playwright `context.routeFromHAR()` (already used by `tests/real-world.tsx`), inferring the main page URL from the HAR (or `--url`). Pending maintainer decisions on CLI surface, URL inference, and the unmatched-request policy. |
| [49-glyph-helper-dispatch.md](49-glyph-helper-dispatch.md) | **DRAFT** — make the renderer actually invoke the native glyph extractors on all platforms (not just macOS). Found that the on-demand release-asset acquisition (DM-393) was never built and `tools/` isn't published, so the helper only works in-repo / via `DOMOTION_HELPER_PATH`. Splits into A (platform-aware resolution, low-risk) + B (the missing cross-platform download/cache/verify layer). Pending a split-or-bundle decision. |
| [59-overlay-schema-ssot.md](59-overlay-schema-ssot.md) | Single source of truth for the overlay / intra-frame-animation shapes (DM-1131): zod base schemas in `src/animation/overlay-schema.ts` derive the renderer's runtime types via `z.infer`, and the declarative-config validator extends the same bases. One base, two views (resolved vs authoring) — a field rename now cascades to both at compile time instead of drifting, and the published JSON Schema stays generated from the same source. |
| [60-programmatic-animate-pipeline.md](60-programmatic-animate-pipeline.md) | The declarative `animate` pipeline (`composeAnimateConfig` / `validateAnimateConfig` / `interpolateConfigVars` / `AnimateConfig`) re-exported from the package root (DM-1130) so library callers run a JSON-config animation in-process instead of shelling out to the CLI. `configDir` made optional (defaults to cwd); the CLI is now a thin file-read wrapper. |
| [61-overlay-resolution-primitive.md](61-overlay-resolution-primitive.md) | `resolveOverlays(page, overlays)` (DM-1132) — lower an overlay's selector `anchor` + typing `maxWidth: "anchor"` into concrete `x` / `y` / `bgWidth` against a live page, for imperative `captureElementTree` + `generateAnimatedSvg` callers. The CLI and the public primitive share one resolution engine (`resolveAnchoredOverlays`); the corner math is shared with `contentBox` via `boxAnchorPoint`. |
| [62-frames-out-animate-pipeline.md](62-frames-out-animate-pipeline.md) | **PROPOSED** (DM-1136) — open up the all-or-nothing `composeAnimateConfig`: a frames-out variant (`composeAnimateFrames` returning the assembled `AnimationConfig` before the final `generateAnimatedSvg`) + an optional `onFrame` per-frame hook, so a consumer who started from a JSON config can inject one bit of custom per-frame manipulation without reimplementing the whole capture→compose loop. The seam already exists; `composeAnimateConfig` collapses to `generateAnimatedSvg(await composeAnimateFrames(...))`. |
| [63-cursor-action-primitives.md](63-cursor-action-primitives.md) | **PROPOSED** (DM-1135) — expose the cursor selector→point resolution (`borderBox` symmetric with `contentBox` + `resolveCursorTarget` sugar) and the declarative action runner (`runActions(page, actions)` + the `AnimateAction` typed union) as public primitives, so imperative callers get the CLI's border-box cursor targeting + DOM-mutation vocabulary. Notes the border-box (cursor) vs content-box (`contentBox`) discrepancy. Companion to doc 62. |
| [64-demo-gallery.md](64-demo-gallery.md) | The **demo gallery** — copy-pasteable examples surfaced across the Astro site's Showcase / Usage pages (`site/src/content/docs/`), drawn from `examples/output`, `examples/output/templates`, the runnable `examples/animate/<demo>/` goldens, and the app captures in `site/demo-assets/apps`. **Rebuilt for DM-1308:** the legacy kerfjs gallery page + per-demo `site/scripts/demos/` folders were removed; the Tier-1 single-capture demos (hero card, pricing table, code block, phone screen) were retired (not re-homed). The animated demos remain: typing search, tab switcher, before/after, terminal onboarding, form fill, scroll-through, + the chained transition-tour. |
| [65-device-chrome.md](65-device-chrome.md) | The `domotion capture --chrome <device>` flag (DM-1206 / DM-1211 / DM-1212) — wrap a capture in a hand-drawn device bezel: `phone` (iPhone-class body + notch + home indicator), `browser` (macOS window: traffic lights + URL pill), `window` (title bar). `--chrome-label` sets the browser URL / window title; `--chrome-theme dark|light` themes the bezel (DM-1212). `src/render/device-chrome.ts` (`wrapInDeviceChrome`) nests the rendered capture rather than re-rendering it (glyph-path fidelity); pure-SVG and cross-platform (the label is the one live `<text>`). |
| [67-terminal-capture.md](67-terminal-capture.md) | The `domotion term --cast <file.cast>` subcommand (DM-1225) — convert a recorded terminal session (asciinema v2) into an animated SVG. Replays the session through a headless VT emulator (`@xterm/headless`) into settle-point frames, renders each as terminal HTML, and stitches them with hard cuts via `generateAnimatedSvg`. Backend in `src/terminal/` (cast parser, emulator snapshot, frame selection, grid→HTML, themes); shipped front-end is asciinema import, live `node-pty` capture is the planned second front-end. |
| [68-live-terminal-capture.md](68-live-terminal-capture.md) | The live `domotion term -- <cmd>` front-end (DM-1226) — capture a real PTY session (optional `node-pty`) instead of a recorded `.cast`, feeding the same VT-emulator → frame → animated-SVG pipeline as doc 67. |
| [69-scroll-button-rendering.md](69-scroll-button-rendering.md) | `::scroll-button(left/right/up/down)` paging arrows (DM-1234) — **supported**. Captures the generated buttons as replica siblings of the scroller: per-side `content` + `:disabled` rules read from the author stylesheet (CSSOM, since `getComputedStyle` can't disambiguate the parameterized pseudo), geometry resolved by a `<body>`-appended replica that — like the real button — takes the **viewport** as its containing block (`top:50%` = 50% of the viewport, not the scroller), enabled/disabled derived from the captured scroll offset. Companion to doc 38's scroll-marker work. |
| [70-template-system.md](70-template-system.md) | The `domotion template <name>` verb + `Template` contract (DM-1276) — **spike shipped**. A template is a parameterized generator that produces a self-contained SVG by driving the existing capture/compose pipeline (no new rendering code), authored in HTML/CSS with arbitrary author-time pre-processing. Built-ins: `lower-third` (generator), `device-mockup` (decorator), `background-loop` (generator, doc 71). Third-party templates are npm `domotion-template-*` packages. Code in `src/templates/`. The wider first-party library + a Lottie input adapter are filed follow-ups. |
| [71-background-loop-template.md](71-background-loop-template.md) | The `background-loop` built-in template (DM-1280) — **shipped**. A procedural seamlessly-looping animated background: a base fill with N soft `radial-gradient` color blobs that each drift (translate) and breathe (opacity), staggered, infinitely. Deterministic from a `seed`; `aurora` / `orbs` variants. Built on the doc-70 contract + doc-44 repeating animations; uses only origin-safe translate + opacity with `alternate` loops (the two animation constraints are documented). |
| [72-kinetic-text-template.md](72-kinetic-text-template.md) | The `kinetic-text` built-in template (DM-1277) — **shipped**. Kinetic typography: a headline string is expanded at author time into per-word / per-char units, each revealed with a staggered one-shot animation (`rise` / `slide` / `fade`), then held assembled. The clearest showcase of the doc-70 "pre-process once, replay free" thesis. Each unit is a transform-wrapper around an opacity-inner (the same two animation constraints as doc 71). |
| [73-template-frames.md](73-template-frames.md) | **Template frames in the `animate` config** (DM-1287) — **shipped**. A frame may set `"template": "<name>"` (+ `"params"`) instead of `input` / `cast` to embed a named template (doc 70) as a nested animated SVG, composing a `lower-third` / `kinetic-text` / etc. into a larger multi-frame animation. Params validated against the template's schema; template inherits the canvas size (centered otherwise). The crux: template frames are pre-rendered before the outer font lifecycle, and the nested document's global names (ids, font families, `.f-N` classes, `@keyframes`, `--scene-dur`) are namespaced per-frame (`namespaceEmbeddedAnimatedSvg`) so they can't collide with the outer animation or sibling template frames. |
| [75-chart-template.md](75-chart-template.md) | **The `chart` built-in template** (DM-1279; multi-series DM-1301; pie/donut DM-1300) — **shipped**. A data/infographics generator: animated `column` / `bar` / `line` / `pie` / `donut` from one or more series (CSV / JSON; `"1,2;3,4"` or `number[][]` for multi-series). `planChart` computes a nice axis max + the px geometry; bars grow from the axis via `scaleY`/`scaleX` + `transformOrigin` (NOT `width`/`height`, which don't apply to the `<g>` wrapper), the line is an inline `<svg>` revealed by a `clipPath` wipe, and pie/donut slices are SVG arc `<path>`s that sweep in (a `.ch-pie-group` scale+rotate + per-slice opacity stagger). Multi-series adds **grouped / stacked** layouts (a stack grows as one `.ch-stack-N` unit), a **legend**, and a **value-axis scale + gridlines** (`yTicks`). |
| [76-social-templates.md](76-social-templates.md) | **The `chat` + `subscribe` built-in templates** (DM-1278) — **shipped**. Two social-media generators: `chat` is a message thread whose bubbles pop in one at a time (each a scale-wrapper about the bubble's anchored corner + an opacity inner, staggered; `me`/`them` alternate sides), parsed from a `{from,text}[]` array or `me:`/`them:` lines. `subscribe` is a follow/subscribe pop-up card that pops in (scale + fade) with a looping `alternate` pulse on the CTA button (light/dark theme, optional bell). Both are timed-reveal intra-frame animation passes — no new rendering code. |
| [74-template-authoring.md](74-template-authoring.md) | **Authoring & publishing a third-party template** (DM-1282) — **shipped**. The hands-on guide for third-party authors: a template is an npm package named `domotion-template-<name>` exporting a `Template` (default / `template` export); the `domotion-svg` peer dep supplies only the types. Covers the package shape, generator vs decorator, the two animation constraints (incl. `transformOrigin`), params/`z.coerce`, `durationMs`, testing via `renderTemplateToSvg`, publishing, and the `domotion-template-*` discovery convention. Runnable scaffold in `examples/template-package/`; public pages at `site/src/content/docs/usage/templates.md` (gallery) + `site/src/content/docs/developer/custom-templates.md` (authoring). |
| [77-nested-animated-compositing.md](77-nested-animated-compositing.md) | **General nested compositing of animated content** (DM-1323) — **design / proposed**. Specifies a primitive for nesting one *animated* composition (cast / scroll capture / template / `animate` result) inside another while preserving its animation — the website→window→OS-context composite. Evaluates feasibility (a probe confirms `<svg>`-in-`<svg>` + transform + the existing `namespaceEmbeddedAnimatedSvg` preserves animation; DM-1319's timeline offset is the per-layer seed). Phased rollout + open decisions; the "decorators are static-only" caveat is the current state. |
| [78-svg-to-image.md](78-svg-to-image.md) | **`svg-to-image`: still SVG → PNG / JPEG / PDF / WebP / AVIF / TIFF** (DM-1353, DM-1354) — **shipped**. A fifth published bin that converts one SVG to a single image file (format from the `-o` extension). The headless, one-shot counterpart to the scrubber's interactive frame export and the still analogue of `svg-to-video`; serves the agent review loop ("render → look at the pixels → critique"). `--at <ms>` samples an animated SVG; `--scale` for retina raster; `--width/--height` contain; PNG/WebP/AVIF/TIFF keep alpha. PNG/JPEG/PDF are native Chromium; WebP/AVIF/TIFF transcode via the bundled `sharp` (lazy-imported). Reuses the `svg-to-video-core` seek+screenshot machinery. |
| [79-harfbuzz-use-reroute.md](79-harfbuzz-use-reroute.md) | **HarfBuzz routing for complex-script edge cases** (DM-1197, DM-1215) — **shipped**. Two narrow uses of real HarfBuzz (harfbuzzjs) where macOS shaping diverges from Chrome's paint: (1) USE-shaped precomposed letters with a canonical base+mark NFD (Kaithi/Balinese/Tulu-Tigalari) shape via HarfBuzz instead of the CoreText helper; (2) an orphaned complex-script combining mark routes through the mark's own font as a HarfBuzz instance so the `U+25CC` dotted circle Chrome inserts + GPOS-positions is reproduced (Adlam/Miao/Brahmi/…). (Renumbered from a duplicate `60-` — DM-1359.) |
| [80-cross-platform-system-fallback-resolver.md](80-cross-platform-system-fallback-resolver.md) | **Cross-platform live system-fallback resolver** (DM-1403) — **macOS / Linux / Windows all shipped + default-on**. The per-codepoint live fallback resolver (macOS CoreText `CTFontCreateForString`) that catches codepoints the static per-block table misses — previously hard-gated darwin-only — generalized to one platform-dispatched entry point. Linux backend via fontconfig `fc-match :charset=<hex>` (calibrated + flipped default-on in DM-1416); Windows backend via DirectWrite `IDWriteFontFallback::MapCharacters` (the win32 glyph helper, calibrated + flipped default-on in DM-1424 — 0 of 4,899 sampled codepoints move). `DOMOTION_SYSTEM_FALLBACK=0` forces the resolver off on Linux/Windows. |
| [93-realistic-typing.md](93-realistic-typing.md) | **Realistic simulated typing** (DM-1518) — **v1 shipped**. The `typing` overlay reveals text character-by-character with the caret glued to the **fontkit-measured** text edge (was ~0.5px/char behind it, from a wrong `0.6em` monospace estimate — on a 25-char line the caret sat ~12.7px behind). One shared per-glyph reveal plan drives both the clip and the caret (can't desync); `step-end` per-keystroke stepping. New params: `mode: "type"\|"paste"` and `jitter: 0–1` (deterministic, seeded). Roadmap: mistake/correct, per-keystroke real-site re-sampling, proportional/glyph-path rendering. |
| [94-interaction-state-capture.md](94-interaction-state-capture.md) | **Interaction-state capture** (`:hover`/`:active`/`:focus`, DM-1516) — **v1 shipped**. A per-frame **`forceState: [{ selector, states }]`** on the `animate` config forces real CSS pseudo-state via CDP `CSS.forcePseudoState` **before** capture, so the frame paints the page's OWN hover/focus styling (cascade siblings like `.card:has(.cta:hover)` included) — not a synthetic overlay. Imperative twin `applyForcedPseudoStates(page, forceState)`. **Load-bearing invariant:** the CDP session must stay attached through the capture (detaching clears the override). Runnable demo `examples/animate/hover-state/`. The doc also enumerates the **auto-detection roadmap** (rest→hover crossfade sugar / computed-style diff / MutationObserver JS-change harness / no-DOM overlay fake) as design options, all reducible to the cross-engine `@keyframes` output constraint. |
| [82-svg-scrubber-review-mode.md](82-svg-scrubber-review-mode.md) | **`svg-scrubber --review`** (DM-1445) — **shipped**. Review mode adds an issue-reporting panel to the scrubber: type a title + note, optionally drag a region over the problem area, and Save — each issue is written as an importable `.ticket` JSON file in the launch directory, capturing the current frame time, the in/out range, and the region (SVG user-units). `title`/`category`/`details` map straight onto `hotsheet_create_ticket`. Endpoint `POST /ticket` (review-only) + the pure `buildTicketFile` builder in `src/scrubber/server.ts`; the region overlay shares the crop overlay's zoom/pan-aware SVG-unit math. The animated-SVG analogue of `svg-review`. **DM-1449** added **multiple regions** per ticket (the overlay stays armed across drags; `regions` array) and an **Attach frame** option that renders the current frame to a sibling `.png` (`framePng`). |
| [81-iframe-recursion.md](81-iframe-recursion.md) | **`<iframe>` recursion into native SVG** (DM-1441 Phase 1, DM-1442 Phase 2) — **both shipped**. A same-origin iframe's document is recursed with the same capture logic and spliced in as the iframe node's child (crisp/scalable/selectable native SVG) instead of a flat raster `<image>`. The inner subtree is placed into the parent's coordinate space by temporarily shifting the shared `vp` origin during the inner walk (composes for nested frames; makes the viewport cull test inner content against the real region), and clipped to the iframe content box via `overflow:hidden`. **Phase 2:** `--cross-origin-frames "*"\|host[:port],…` (config `captureCrossOriginFrames`) recurses allowlisted cross-origin frames by launching Chromium with web security disabled (default off + stderr security warning; matching in `cross-origin.ts`, blast-radius-limited to the allowlist). Updates raster-fallback §E4. |
| [89-storyboard-sequencing.md](89-storyboard-sequencing.md) | **Storyboard sequencing** (DM-1527) — **shipped (core)**. A `domotion storyboard <config.json>` verb that sequences DISTINCT scenes end-to-end into ONE animated SVG with an inter-scene transition between each (title card → capture/demo → CTA). Each scene is a `template`, a live `capture` (url/file), a `cast`, or a pre-rendered `svg`; each renders to a self-contained (possibly animated) SVG that's namespaced, placed, and timeline-re-anchored, then sequenced through `generateAnimatedSvg`. Reuses the existing template-frame/cast-frame embedding + the animator's transition enum (crossfade/cut/push-left/scroll — no reinvention). Exports to MP4 via `svg-to-video`. Published JSON Schema; reveal transitions + cross-scene font dedup are follow-ups. |
| [87-format-presets.md](87-format-presets.md) | **Format presets** (DM-1521) — *design*. A `--format` flag (reel/story 9:16, square 1:1, portrait 4:5, landscape 16:9, or raw `WxH`) that sets the canvas + a safe-area inset, so one flag yields a platform-ready asset. Precedence: explicit width/height > format > template default. Shared `FORMATS`/`resolveFormat`; composes with the brand kit. Implementation (DM-1534) + per-template responsive reflow + capture/animate support are follow-ups. |
| [86-creative-template-pack.md](86-creative-template-pack.md) | **Creative template pack** (DM-1523) — *design*. Narrative building blocks as ordinary templates: `title-card`, `quote`/testimonial, `caption` strip (for compositing over content), `counter`/countdown, `stat`/KPI callout, `compare` (before/after wipe), `cta`/end-card. Number animation uses **odometer digit-reels** (pure `translateY`, cross-engine-safe — no per-frame text). Compose with brand kit (DM-1522) + format presets (DM-1521). Impl split into Batch A/B/C follow-ups (DM-1531/1532/1533). |
| [85-brand-kit.md](85-brand-kit.md) | **Brand kit** (DM-1522) — *design*. A reusable `brand.json` of design tokens (palette / font / radius / logo / background) that every template reads for its **defaults** so output is on-brand at scale. Precedence: explicit param > brand token > built-in default (merged before zod defaults). Each template exposes `brandDefaults(brand)`; `--brand <file>` on `domotion template`. Implementation + logo-slot + capture/animate theming are follow-ups. |
| [84-viewer-browser-support.md](84-viewer-browser-support.md) | **Viewer browser-support matrix** (DM-1515) — the contract for browsers that *view* the output (distinct from capture-platform support). **Blink** (Chrome/**Edge**/Brave/Opera/Electron) + **WebKit** (Safari + all iOS browsers) are **first-class**; **Gecko** (Firefox) is **best-effort** with a documented OMTA compositor **desync-under-load** caveat (paired opacity/transform tracks can drift once Firefox demotes some animations to the main thread). Covers the CSS-vs-SMIL rationale (one timeline, GPU-composited, reduced-motion gating; never mix clocks — DM-1507), the `<img>` embedding constraint (CSS+SMIL animate there, JS/WAAPI do not), and the generator-side per-unit fuse mitigation (DM-1512/1513). Stress-test with `examples/output/stress-gallery.html` (DM-1514). |
| [104-overlay-windows.md](104-overlay-windows.md) | **Per-overlay windows + per-state overlays in a compressed run** (DM-1767, DM-1796) — **shipped**. Also owns **how a typing overlay LEAVES its window** (§3.1, DM-1796): it holds at full opacity through the window's end and exits the way its frame does — a hard cut at a `cut` boundary or a compressed-run state snap (the seamless handoff to the next frame's real captured text), a dissolve across a non-`cut` transition, and the historical 150 ms-early fade only on the scene's last frame where nothing takes over. The old everywhere-pre-fade left the value visible on NEITHER side of a handoff for ~120 ms ("the input value disappears then reappears"). Two changes to the overlay MODEL. (1) **`endAt`** on every overlay kind: the ms from frame start at which that overlay's window closes, instead of the frame's end — clamped to the frame, so an overlay may end early but never leak across the cut; plus a `delay` on the `svg` kind so all six share one explicit `[delay, endAt]` window. (2) **Per-state `overlays`** on a `states:` run — anchor-resolved while the page is at that state (not the run's last) and re-based onto the frame's timeline, its `delay` shifted by the state's offset and `endAt` pinned to the state's end. Together they remove `overlays` from `autoCompress`'s exclusion set: an overlay-carrying run now collapses whole, reproducing the authored behavior exactly (`form-fill` 4→2 frames −12%, `editor-session` 11→6 −15%, both pixel-identical). |
| [105-asymmetric-harness-browsers.md](105-asymmetric-harness-browsers.md) | **Asymmetric capture-vs-raster browsers in the visual harnesses** (DM-1790) — **shipped**. Both harnesses drove ONE Chromium for the expected paint AND the candidate SVG's rasterization, so a launch flag meant for one side silently moved both — making a capture-only experiment unmeasurable (docs/66: `--font-render-hinting=none` reads as an improvement in embedded mode purely because both sides go soft together). `DOMOTION_CAPTURE_FLAGS` / `DOMOTION_RASTER_FLAGS` now configure the two sides independently: neither set ⇒ one browser (unchanged default), capture only ⇒ asymmetric (the consumer's condition), both identical ⇒ the historical coupled behavior stated deliberately. Folds the capture flags into the expected-PNG cache key (without which a flagged run silently reuses the unflagged cached screenshot) and records the condition beside the results. Validated on Linux: three configurations, three genuinely different numbers, with the asymmetric one NOT between the other two. |
| [106-blink-font-parity-inventory.md](106-blink-font-parity-inventory.md) | **Blink font-resolution parity inventory** (DM-1854) — **in progress**. Stage-by-stage map of what Blink does vs what we do, against the in-repo Chromium checkout (`external/chromium` @ 7d859f27). Records Blink's fallback stage machine (`kFallbackPriorityFonts` is one-shot; `kPreferencesFonts` is dead; the terminal is Times + the FIRST candidate's notdef, not a tofu font), and that `kSystemFonts` is **three different procedures** — macOS asks CoreText with the run's current font as base, Linux asks fontconfig keyed on locale, Windows consults a hardcoded per-script table BEFORE DirectWrite. Headline finding: our `fallbackFontChain` per-block table **has no Blink counterpart** and answers before the OS is ever asked — the structural root of the 2026-07 wrong-font cycle. Rasterization is explicitly out of scope (accepted hinting floor); shaping is in scope but tracked separately. |
| [107-font-conformance-oracle.md](107-font-conformance-oracle.md) | **Font-resolution conformance oracle** (DM-1854) — **shipped**. `tools/font-conformance.ts` / `npm run fonts:conformance`. Parity cannot be established by a fixture suite (fixtures sample; a wrong-font bug lives in the codepoints no fixture covers), so this enumerates instead: every assigned Unicode codepoint (292,466, derived from Node's ICU tables) × every font stack the fixture corpus actually uses (418 combinations over 205 stacks, extracted from the 1,115 fixtures' COMPUTED styles). Asks Chrome via CDP `CSS.getPlatformFontsForNode` and `resolveFontForCodepoint` the same question, exits non-zero on any mismatch, allowlist entries require a written reason. Agreement is tiered (exact / same-file / documented alias) and counted separately. Read `distinctMismatchPairs` next to `mismatchTotal` — the former is how many DECISIONS are wrong, the latter is blast radius. |
| [108-shaping-conformance-oracle.md](108-shaping-conformance-oracle.md) | **Shaping conformance oracle** (DM-1856) — **shipped**. `tools/shaping-conformance.ts` / `npm run fonts:shaping`. The sibling of doc 107: that one proves *which font*, this proves *which glyphs, where*. Ligatures, contextual forms, Indic reordering, Arabic joining and mark attachment are all invisible to a face-identity oracle — a mark placed 3px wrong scores as a clean pass. Compares Chrome's per-face glyph counts (CDP) plus per-character geometry (`Range.getClientRects()`) against the `<text x="…">` position list the renderer actually emits. Neither side exposes glyph IDs, so that gap is documented rather than papered over. Tiered: exact / positions-differ / positions-not-comparable / count-mismatch, with the positions-differ tier recorded in full because it is the mark-offset detector. Baseline 2,385 runs: 84.8% exact, **348 runs with real position disagreements** (median 1.20px, max 5.64px). |
| [113-cluster-granularity-fallback.md](113-cluster-granularity-fallback.md) | **Shaped-cluster font fallback** — **shipped, default-on**. `tools/cluster-conformance.ts` / `npm run fonts:cluster-conformance` is the multi-codepoint face oracle that the per-codepoint sweep cannot be: it compares Chrome's painted face set with the production glyph-path run splitter on partially covered base+mark clusters. The default arm must be green; `DOMOTION_CLUSTER_FALLBACK=0` deliberately turns the four fixed mid-cluster cases red, proving the instrument exercises the mechanism. Its partial-webfont cell is graded with the retained webfont bytes supplied as CoreText's cascade base. |
| [114-angled-linear-wipe.md](114-angled-linear-wipe.md) | **Parameterized linear wipe angle** — **shipped** (DM-2041). `transition.wipeAngle` rotates the linear reveal direction clockwise from the byte-identical left-to-right default; arbitrary angles use fixed-eight-vertex sampled polygon clips for cross-engine interpolation. |
| [115-parameterized-transition-design.md](115-parameterized-transition-design.md) | **Parameterized transition architecture** — investigation complete (DM-2042). Recommends a shared discriminated schema, legacy-name compatibility normalizer, one internal viewer-safe motion plan, typed built-in parameters, then a bounded custom recipe. Follow-ups: DM-2070/2071/2072. |
| [116-transition-schema-and-normalization.md](116-transition-schema-and-normalization.md) | **Shared transition schema + compatibility normalization** — **shipped** (DM-2070). One zod contract now derives the programmatic type, validates animate/storyboard, and generates both JSON Schemas; legacy names normalize to bounded opacity/translate/scale/clip/overlay plans before renderer classification. |
| [117-parameterized-built-in-transitions.md](117-parameterized-built-in-transitions.md) | **Parameterized built-in transition families** — **shipped** (DM-2071). Strict `push`, `reveal`, and `zoom` family forms plus bounded `shine` parameters support angles, distances, viewport-relative origins, radii, scales, bands, colors, and opacity while legacy names retain identical defaults. |
| [118-custom-transition-recipes.md](118-custom-transition-recipes.md) | **Declarative custom transition recipes** — **shipped** (DM-2072). Strict incoming/outgoing opacity, viewport translate, scale, incoming clip, and supported shine primitives compose on one CSS timeline, with explicit ownership, z-order, reduced-motion, loop, and unsupported-combination semantics—no raw viewer code. |
| [119-degraded-font-resolution.md](119-degraded-font-resolution.md) | **Degraded font-resolution contract** — helperless rendering remains deterministic and functional, but explicitly relinquishes Chromium-exact installed-family, fallback, trait, axis, and pixel answers; inventories every degraded seam and pins live-before-static ordering. |
| [120-same-machine-text-parity-contract.md](120-same-machine-text-parity-contract.md) | **Normative same-machine Chromium text parity contract** — logical face, shaping, and layout equality; staged verdicts; environment fingerprints; representative and exhaustive gates. |
| [121-font-cache-parity.md](121-font-cache-parity.md) | **Font cache identity and lifetime inventory** — every production resolver, cut, coverage, native-helper, shaping, webfont, document-order, generation, and environment-derived cache mapped to its key, reset boundary, and Chromium analogue. |
| [124-embedded-font-build-diagnostics.md](124-embedded-font-build-diagnostics.md) | **Embedded-font build diagnostics** — per-entry source, builder, disqualification reasons, retained tables, and glyph/run counts in visual-test artifacts. |
| [125-windows-unicode-font-route-trace.md](125-windows-unicode-font-route-trace.md) | **Windows Unicode font-route trace** — per-cell Chromium and Domotion declared-family, hardcoded-candidate, DirectWrite, final-face, and glyph-id evidence for focused CI runs. |
| [126-backdrop-filter-isolation.md](126-backdrop-filter-isolation.md) | **Backdrop-filter isolation** — **shipped narrow boundary** (DM-2171/DM-2357/DM-2490). Chromium-composited target snapshots preserve direct backdrop sampling and vector descendants; exact host and generated-pseudo materializations are silent, while retained partial/unavailable fallbacks emit named status diagnostics. Doc 187 records the remaining partial Backdrop Root/effect-space transitions. |
| [127-border-outline-phase-oracle.md](127-border-outline-phase-oracle.md) | **Border/outline device-pixel phase oracle** (DM-2184/DM-2321/DM-2355/DM-2491) — 128 isolated HTML-versus-img-rendered-SVG cases across paint kind, style, width, and quarter-pixel phase, parameterized over CSS zoom and device scale. Separates snap geometry from alpha profiles; doc 191 ratifies the complete source-exact matrix as a strict three-platform gate. |
| [128-chromium-unicode-decision-audit.md](128-chromium-unicode-decision-audit.md) | **Chromium Unicode decision audit** (DM-2254) — inventories every Unicode/font-routing predicate, distinguishes cited Chromium/HarfBuzz transcriptions from Domotion heuristics, and defines the pinned ICU 78.2 companion contract used by the helper-backed supported path. |
| [129-chromium-parity-verification-program.md](129-chromium-parity-verification-program.md) | **Chromium parity verification program** — the durable cross-renderer audit and validation plan: upstream decision transcription, exact stage oracles, activation controls, generated transition/metamorphic coverage, platform fingerprints, and logically classified visual review. |
| [130-paint-geometry-oracle.md](130-paint-geometry-oracle.md) | **Gradient, mask, clip, and resizer geometry oracle** — 110 source-transcribed logical rows are exact, including URL-mask round/space origin-phase versus clip-destination ownership and destructive mutation controls, without a tolerance increase. |
| [131-transform-geometry-oracle.md](131-transform-geometry-oracle.md) | **Transform geometry oracle** — live Chromium affine corner/origin comparisons, projective raster-boundary activation, affine `matrix3d()` negative control, and reflection direction/offset records. |
| [132-paint-order-oracle.md](132-paint-order-oracle.md) | **Paint-order transition oracle** — stacking-context creators, CSS Appendix E buckets, descendant hoisting/atomic containment, and live Chromium hit-stack records. |
| [133-replaced-geometry-oracle.md](133-replaced-geometry-oracle.md) | **Replaced/control/generated geometry oracle** — live painted object-fit rectangles, native-control ownership boundaries, and captured `::before`/`::after` boxes. |
| [134-raster-boundary-oracle.md](134-raster-boundary-oracle.md) | **Raster-fallback activation oracle** — paired positive/vector controls across every live fallback class, helper-disabled behavior, recursive frames, and retired text rasters. |
| [149-macos-terminal-raster-evidence.md](149-macos-terminal-raster-evidence.md) | **macOS terminal-raster evidence** — exact same-record activation predicates, edge envelope, and zero-diff controls for native CoreText mask residuals. |
| [197-paths-native-raster-floor.md](197-paths-native-raster-floor.md) | **Paths/native raster floor matrix** — fingerprinted three-OS lossless evidence, exact logical-first adjudication, and explicitly unratified per-dimension envelopes that cannot alter scalar visual caps. |
| [135-corner-shape-superellipses.md](135-corner-shape-superellipses.md) | **CSS corner-shape superellipses** — computed physical shape capture plus Blink's parameter/exponent mapping, fitted cubic contour, concave inversion, opposite-hull constraint, and inset alignment as vector SVG. |
| [136-semantic-coverage-inventory.md](136-semantic-coverage-inventory.md) | **Semantic transition coverage inventory** — source, production owner, oracle, metamorphic test, visual fixture, platform, and explicit uncovered-state links for every parity area. |
| [137-specialized-path-activation-ledger.md](137-specialized-path-activation-ledger.md) | **Specialized-path activation ledger** — positive, negative, mutation, and visual evidence for renderer mechanisms that ordinary line coverage cannot prove. |
| [138-parity-release-gate.md](138-parity-release-gate.md) | **Parity release gate** — one machine-readable verdict over semantic coverage, activation, platform fingerprints, and all-platform evidence. |
| [139-raster-fallback-content-gates.md](139-raster-fallback-content-gates.md) | **Raster fallback content gates** — browser E2E checks that each supported raster boundary contains the intended Chromium-painted pixels, not merely an activated image node. |
| [140-demo-review-stage-evidence.md](140-demo-review-stage-evidence.md) | **Demo-review stage evidence** — CI stage reports and runner fingerprints linked to fixtures without pixel-score inference, with mandatory persisted human classification in filed tickets. |
| [141-unified-shaping-evidence.md](141-unified-shaping-evidence.md) | **Unified shaping evidence** — one source-honest record joining Chromium painted faces/origins with helper face/file/axes and exact HarfBuzz glyph, cluster, advance, and offset data, guarded by per-input controls. |
| [142-renderer-font-route-audit.md](142-renderer-font-route-audit.md) | **Renderer font-route audit** — source-backed inventory of every production face-assignment and emission owner, the exact scope of the standalone resolver oracle, and the required per-run provenance gate. |
| [143-production-text-run-provenance.md](143-production-text-run-provenance.md) | **Production text-run provenance** — opt-in per-span assignment/source/shaping/emitter ledger plus a Chromium-joined renderer-route oracle and composite demo-review gate. |
| [144-dotted-circle-shaping-evidence.md](144-dotted-circle-shaping-evidence.md) | **Dotted-circle shaping evidence** — Chromium paint probes and selected-face HarfBuzz glyph streams replace block, plane, and Tamil-pair routing gates. |
| [146-macos-ideograph-fallback-cache-audit.md](146-macos-ideograph-fallback-cache-audit.md) | **macOS ideograph fallback-cache audit** — exact Chromium key, renderer-thread lifetime, and the remaining Domotion model gaps. |
| [148-sourcegraphic-url-filter-surfaces.md](148-sourcegraphic-url-filter-surfaces.md) | **Blink SourceGraphic ownership for HTML URL-filter convolution** (DM-2415) — `feConvolveMatrix` graphs preserve Chromium's layer-space source pixels, reference-region crop, and Skia tile semantics as one narrow raster surface; ordinary URL filters and native SVG remain vector. |
| [155-animated-viewbox-culling-audit.md](155-animated-viewbox-culling-audit.md) | **Animated viewBox culling geometry audit** (DM-2386/DM-2460/DM-2461) — pinned Blink/Skia reference-box, operation, visual-bound, timing-composition, motion-path, and projective decisions; renderer-owned fill/stroke/view and visual-surface facts plus function-first composed global-timeline swept bounds are implemented; DM-2462 owns the independent full live oracle. |
| [156-broken-image-fallback-ownership-audit.md](156-broken-image-fallback-ownership-audit.md) | **Broken-image fallback ownership/capture/rendering** — Blink's UA-shadow state machine, exact 18 px threshold, title/AX semantics, normal shaped text route, and shared 1×/2× bitmap boundary; DM-2463/2464 ship pierced-CDP facts plus vector container/text and an isolated fingerprinted icon raster, while DM-2465 hard-gates 27 live cases × DPR 1/2 × light/dark independently on macOS/Linux/Windows with direct RGBA icon proof and mutations. |
| [157-pseudo-generated-fragment-geometry-audit.md](157-pseudo-generated-fragment-geometry-audit.md) | **Generated pseudo fragment and baseline geometry audit** (DM-2383) — Blink-owned anonymous content children, line fragments, visual bidi order, vertical alignment, writing transforms, fragmentation, the exact capture schema, and the DM-2466/2467/2468 dependency sequence. |
| [158-authoritative-control-pseudo-cascade.md](158-authoritative-control-pseudo-cascade.md) | Chromium-authoritative cascade capture for legacy WebKit control pseudos: pierced UA-shadow ComputedStyle, origin activation, anonymous resizer handling, and live specificity/layer/scope/condition/adopted/shadow/state gates. |
| [159-exact-text-transform-geometry-audit.md](159-exact-text-transform-geometry-audit.md) | **Exact transformed-text geometry audit** — proves the cumulative diagonal/geometric-mean font scale is not Blink's model and documents the shipped DM-2469 same-frame CDP capture: ordered neutral/live fragment quads, complete signed matrices, shaped positions/baselines, zoom separation, exact restoration, and fail-closed projective ownership; renderer consumption and all-platform ink gating remain DM-2470/2471. |
| [160-chromium-roll-differential-audit.md](160-chromium-roll-differential-audit.md) | Old-versus-new structured parity-stage and representative-visual audit for Chromium/Skia/HarfBuzz/ICU rolls, with comparable fingerprints and mandatory source-review classification. |
| [161-parity-blind-spot-corpus.md](161-parity-blind-spot-corpus.md) | Stable cross-platform html-test fixture pack for SVG effects, masks/clips, opaque-origin iframes, fragmentation, bidi/emoji, MathML, controls, projective transforms, filters/blends, zoom, and interacting fallbacks. |
| [164-cross-origin-scroll-frame-capture.md](164-cross-origin-scroll-frame-capture.md) | Threads the opt-in cross-origin iframe allowlist and launch contract through every live scroll segment, preserving native recursion only for allowed hosts. |
| [162-inline-svg-3d-transform-audit.md](162-inline-svg-3d-transform-audit.md) | **Cloned inline-SVG 3D transform boundary** — proves Blink's SVG-child affine flattening versus root/HTML projective ownership; ships exact used-affine freezing, CDP-quad outer clone/`foreignObject` promotion, one-owner emission, and a fingerprinted macOS/Linux/Windows two-leg Chromium-versus-generated-SVG gate at DPR 1/2 with six mutation controls. |
| [163-url-background-image-geometry-audit.md](163-url-background-image-geometry-audit.md) | **URL background image geometry** (DM-2370/2477/2478/2479/2365/2480) — shipped selected-image/natural sizing, 1/64 px Blink tile geometry, attachment and stitched-fragment ownership, plus a strict 26-row native macOS/Linux/Windows DPR-1/2 gate with warning/palette/pattern/mutation blockers and lossless fingerprinted evidence. |
| [165-native-scrollbar-layout-paint-ownership-audit.md](165-native-scrollbar-layout-paint-ownership-audit.md) | **Native scrollbar layout/paint ownership audit** (DM-2368) — pinned Blink/custom-theme/platform-native/Skia boundary, a Playwright `--hide-scrollbars` false-negative control, strict overflow/axis/RTL/writing/clip/zoom/DPR evidence, and the exact custom-vector versus stock-raster contract. |
| [166-animated-culling-geometry-oracle.md](166-animated-culling-geometry-oracle.md) | **Animated culling geometry oracle** — independent unculled-versus-production final-SVG timeline gate over reference boxes, frozen/live/nested/projective transforms, timing, effects, clips/masks, narrow crossings, fail-closed channels, mutation controls, and fingerprinted macOS/Linux/Windows DPR/zoom runs. |
| [167-native-control-source-frame-rasters.md](167-native-control-source-frame-rasters.md) | **Source-frame-coherent native-control rasters** — one authoritative compositor frame plus one atomic transparent isolation frame, alpha-proven source pixels, overlap/clip/DPR validation, named capture failures, and strict no-synthesis emission. |
| [168-sfns-mask-baseline-oracle.md](168-sfns-mask-baseline-oracle.md) | **SFNS CoreText mask/baseline oracle** (DM-2452) — exact-byte macOS Chromium/CoreText/Domotion comparison at shared gids and quarter-pixel origins, with separate native metrics/baseline evidence, cold/warm stability, full-axis integrity, and an `opsz=26` activation control; diagnostic only, with no renderer tolerance change. |
| [169-authoritative-scrollbar-capture.md](169-authoritative-scrollbar-capture.md) | **Authoritative Blink scrollbar capture and paint** (DM-2481/2482/2483) — live marker/overlay-ink geometry, ordered custom vector boxes, same-frame platform-native strip/corner crops with fingerprints and SHA provenance, explicit faded absence, exact DPR-2 source/output oracle, and no legacy 7 px synthesis. |
| [170-blink-effective-appearance-routing.md](170-blink-effective-appearance-routing.md) | **Blink EffectiveAppearance ownership** (DM-2453) — CDP-resolved author background/border origins, rollback/layer/animation handling, exact auto and author-style switches, conservative warning/raster failure, and a native all-platform activation matrix. |
| [171-closed-shadow-control-decorations.md](171-closed-shadow-control-decorations.md) | **Source-owned closed-shadow control decorations** (DM-2455) — exact pierced-UA part identity/geometry, one transparent isolation atlas, reversible select-inner isolation, fail-closed overlays, Blink paint phase, and macOS/Linux/Windows state/axis/zoom/DPR gates. |
| [172-native-file-selector-button.md](172-native-file-selector-button.md) | **Native file-selector button ownership** (DM-2454) — child EffectiveAppearance routing, exact closed-UA button crop, localized source-shaped status text, native/author split, reversible isolation, and source paint order. |
| [173-native-scrollbar-release-gate.md](173-native-scrollbar-release-gate.md) | **Native scrollbar release gate** (DM-2484) — schema-v2 three-platform/DPR/zoom evidence contract, lossless strip SHA verification, strict custom/native adjudication, mutation controls, and an intentionally red stock-native/platform-evidence boundary. |
| [174-html-mask-origin-clip-geometry.md](174-html-mask-origin-clip-geometry.md) | **Independent HTML mask-origin / mask-clip geometry** (DM-2472/DM-2494) — pinned Blink positioning-versus-painting rectangles across border/padding/content/no-clip, cyclic URL layers, explicit/contain/cover/repeat/round/space, zoom, writing modes, slice/clone fragments, same-box controls, and DPR-1/2 mutation-gated browser oracles. |
| [175-pseudo-fragment-protocol-oracle.md](175-pseudo-fragment-protocol-oracle.md) | **Chromium pseudo-fragment protocol oracle** (DM-2466) — source-transcribed DOMSnapshot/content-quad decoder for anonymous content items, UTF-16/visual fragments, fragmentainer translations, slice/clone edges, shaped advances, and horizontal/vertical/sideways paint baselines; 80 live DPR rows and ten mutation controls pass without changing production. |
| [176-source-owned-pseudo-fragment-capture.md](176-source-owned-pseudo-fragment-capture.md) | **Source-owned generated-pseudo fragment capture** (DM-2467) — production frame-scoped CDP prepass sharing the DM-2466 decoder, with selected-face typography, exact physical baselines/edges, same-origin-frame correlation, no clone/host-anchor fallback, and one isolated Chromium pseudo surface on protocol ambiguity. |
| [177-affine-text-paint-consumption.md](177-affine-text-paint-consumption.md) | **Exact affine text paint consumption** (DM-2470) — consumes transform-neutral shaped fragments through one complete signed residual matrix across all text paint branches, removes scalar transform compensation, and retains explicit Chromium surfaces for projective, generated-line-clamp, or unavailable facts. |
| [178-direct-pseudo-fragment-rendering.md](178-direct-pseudo-fragment-rendering.md) | **Direct source-owned generated-pseudo rendering** (DM-2468) — paints captured anonymous text/image and slice/clone box fragments in explicit host stacking slots without legacy host anchors, with a four-device-pixel DPR-1/2 Chromium-versus-SVG gate on macOS, Linux, and Windows. |
| [179-transformed-text-all-platform-gate.md](179-transformed-text-all-platform-gate.md) | **Transformed-text all-platform parity gate** (DM-2471) — exact independent CDP fragment/matrix facts plus fixed device-space Chromium-versus-SVG ink/content checks over 25 cases at DPR 1/2 on macOS, Linux, and Windows, with eight mandatory mutations and fingerprinted artifacts. |
| [180-source-owned-summary-disclosure-markers.md](180-source-owned-summary-disclosure-markers.md) | **Source-owned summary disclosure markers** (DM-2457) — Chromium `::marker` identity plus DOMSnapshot paint-fragment geometry, Blink's exact size/orientation path, retired details-box approximation, suppression controls, and a macOS/Linux/Windows DPR-1/2 ink gate. |
| [181-background-clip-text-url-color-semantics.md](181-background-clip-text-url-color-semantics.md) | **URL/color `background-clip:text` closure** (DM-2366) — Blink bottom-layer color/image order, alpha text/stroke masks, selected URL tile geometry, descendant ownership, stitched slice/clone fragments, and a vector-only strict DPR-1/2 live gate. |
| [182-native-control-fallback-retirement.md](182-native-control-fallback-retirement.md) | **Native-control sampled-fallback retirement** (DM-2458) — strict EffectiveAppearance routing, deleted macOS palette/geometry/constants, warning-only required-raster failure, structural source-fact requirements, mutation gate, and fingerprinted macOS/Linux/Windows activation workflow. |
| [183-composed-metamorphic-parity-corpus.md](183-composed-metamorphic-parity-corpus.md) | **Composed real-world and metamorphic parity corpus** — seven cross-decision repository visuals, a pinned html-test page, six live-validated relations, capture-frozen canvas and native iframe ownership, fresh-process visual isolation, and a hard fingerprinted macOS/Linux/Windows workflow. |
| [184-replaced-ownership-transition-matrix.md](184-replaced-ownership-transition-matrix.md) | **Replaced/native ownership transition matrix** (DM-2364) — 42 source-owned rows per DPR for object sizing/zoom, decode fallback, canvas/video frames, native→author controls, platform states, and generated boxes, with a strict paired adjudicator and fingerprinted macOS/Linux/Windows workflow. |
| [185-blend-filter-pixel-stage-oracle.md](185-blend-filter-pixel-stage-oracle.md) | **Blend/filter pixel-stage ownership** (DM-2360/DM-2485) — pinned Blink/Skia filter, premultiplied blend, and legacy/modern alpha-precision rules; 17 blend modes, ordered shorthand kernels, composed isolation/source-surface fixtures, six explicit vector/raster boundaries, three destructive mutations, and a strict source-exact macOS/Linux/Windows DPR-1/2 gate with an unchanged four-code stage bound. |
| [186-animated-3d-frame-state-parity.md](186-animated-3d-frame-state-parity.md) | **Animated CSS 3D frame-state parity** (DM-2359) — strict prepass-wide timeline seeking, exposed same-frame CDP quad/computed composition/residual/raster-owner facts, rotate3d/translate3d/matrix3d/perspective/origin/preserve/grouping/additive rows at four times, five mutations, and a fingerprinted macOS/Linux/Windows DPR-1/2 gate. |
| [187-backdrop-source-surface-transitions.md](187-backdrop-source-surface-transitions.md) | **Backdrop-filter source-surface transitions** (DM-2357) — pinned Blink/Skia Backdrop Root ownership, 17 font-free live families at DPR 1/2, independent paint-order facts, under/final-crop mutation controls, and scoped follow-ups for ancestor effect space, generated pseudos, fixed ordering, and stale diagnostics. |
| [192-fixed-backdrop-paint-order.md](192-fixed-backdrop-paint-order.md) | **Fixed backdrop paint order** (DM-2489) — source-correct marker/overflow stacking discrimination and independent DOMSnapshot DPR-1/2 proof that fixed raster, retained target vectors, and later siblings keep Chromium order. |
| [193-generated-pseudo-backdrop-source-ownership.md](193-generated-pseudo-backdrop-source-ownership.md) | **Generated-pseudo backdrop source ownership** (DM-2488) — pseudo-backend DOMSnapshot isolation, Chromium prior-device raster ownership at exact before/after/checkmark and z-order slots, retained box/text/image vectors, under/final-crop mutations, and a macOS/Linux/Windows DPR-1/2 gate. |
| [194-ordinary-backdrop-effect-space.md](194-ordinary-backdrop-effect-space.md) | **Ordinary backdrop effect-space ownership** (DM-2487) — serializes pinned-Blink nearest-root and ancestor-effect facts, reversibly removes SVG-reapplied effects during Chromium crop capture, closes filter-root replay, and records the unchanged-threshold opacity/transform/mask/blend/target-filter residuals without promoting a partial native gate. |
| [195-strict-ordinary-backdrop-ownership.md](195-strict-ordinary-backdrop-ownership.md) | **Strict ordinary backdrop ownership** — atomic opacity/blend/mask roots, a source-owned rotate/skew terminal surface, document-root scroll/sticky ownership, guarded source-edge raster-floor adjudication, and a strict native macOS/Linux/Windows DPR-1/2 workflow without threshold changes. |
| [188-generated-svg-effect-combinations.md](188-generated-svg-effect-combinations.md) | **Generated SVG effect combinations** (DM-2358) — 24 deterministic cases cover all 953 pairs among 18 gradient/mask/clip/native-SVG axes, preserve stylesheet-owned marker/non-scaling-stroke/interpolation classes without taking geometry ownership, and run exact capture-to-SVG pixels plus three destructive transitions at DPR 1/2 on macOS/Linux/Windows. |
| [189-nested-projective-context-ownership.md](189-nested-projective-context-ownership.md) | **Nested projective-context raster ownership** (DM-2356/DM-2492/DM-2493) — source-derived used-context owners across 25 families and four environment profiles, strict schema-v2 lossless evidence, nine mutations, and a macOS/Linux/Windows DPR-1/2 aggregate release gate. |
| [191-cross-platform-border-phase-envelopes.md](191-cross-platform-border-phase-envelopes.md) | **Ratified cross-platform border/outline phase envelopes** (DM-2355/DM-2491) — pinned source ownership, reviewed macOS/Linux/Windows DSF/zoom artifacts, strict runner/corpus/artifact fingerprints, narrow unchanged paint profiles, and complete 128-row coverage including uniformly snapped double-border stripes. |
| [196-linux-arm64-release-parity.md](196-linux-arm64-release-parity.md) | **Linux arm64 published-helper and parity gate** (DM-2353) — clean-cache acquisition of pinned glyph/ICU release assets on a native Noble arm64 runner, triple release checksum plus ratified fingerprint and ELF validation, live helper protocols/cache reuse, exact logical gates, known-exact HTML/Unicode visuals, recursive artifact hashing, and a fail-closed aggregate verdict. |

The docs preserve their historical SK-XXXX references — those are the slicekit Hot Sheet tickets that originally drove each feature. Treat the ticket numbers as opaque pointers to design context, not as live work tracking.
