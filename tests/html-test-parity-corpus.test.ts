import { createHash } from "node:crypto"; import { readFileSync } from "node:fs"; import { describe,expect,it } from "vitest";
const manifest=JSON.parse(readFileSync("tools/html-test-parity-corpus.json","utf8")) as {fixtures:Array<{id:string;file:string;sha256:string;categories:string[]}>};
describe("html-test parity corpus",()=>it("has stable unique identities and complete blind-spot coverage",()=>{const ids=manifest.fixtures.map(x=>x.id);expect(new Set(ids).size).toBe(ids.length);const categories=new Set(manifest.fixtures.flatMap(x=>x.categories));for(const category of ["svg-effect-boxes","css-mask-clip","cross-origin-scroll-iframe","fragmented-background-table","bidi-emoji","mathml","native-controls","projective","filters-blends","zoom","interacting-fallbacks"]) expect(categories.has(category)).toBe(true);for(const fixture of manifest.fixtures){const bytes=readFileSync(fixture.file);expect(bytes.length).toBeGreaterThan(1000);expect(createHash("sha256").update(bytes).digest("hex")).toBe(fixture.sha256)}}));
