/**
 * Compatibility export for the source-linked DM-2583 probe contract.
 * The schema owns the data so the collector and pure adjudicator cannot drift.
 */
export {
  ANIMATED_IMAGE_TRUTH_PROBE_REQUIREMENTS,
  type AnimatedImageTruthProbeRequirement,
} from "./animated-image-owner-resource-truth-schema.js";
